# 主动消息机制学习笔记（心跳 · 链式 · 回访）

> 整理日期：2026-08-02
> 主题来源：一篇关于"AI 主动发消息机制"的技术文章 + 与 AutoClaw 的深度讨论
> 关联实现：桌宠 v6.18「主动关心系统」（链式唤醒 + 回访 + 状态感知）
> 阅读对象：李滨宇（用于以后回顾 AI Agent 基础概念）

---

## 一、背景：文章的由来

作者六月份出了教程，讲"AI 主动发消息"的解决方案（此前没人系统讲过）。之后有人拿截图宣称自己实现并吸引流量，但评论区至今还有大量人解决不了这个问题，于是作者重新写一篇讲解。

**核心论点**：心跳很原始；真正的主动 = AI 自己决定什么时候醒来（链式）+ 对话后短期回访（回访）。

---

## 二、文章原文精华

### 2.1 心跳是什么（原始比喻）

> AI 像一个一直在睡觉的人。小明找 AI 都是先拍醒再说话，说完 AI 继续睡。
> 有一天小明希望 AI 主动找他，于是委托"系统"："每隔 15 分钟去喊醒 AI，让他判断要不要来找我。"
> 每 15 分钟系统拍醒 AI，从用户视角看就是 AI 主动来了——这就是**心跳**。

### 2.2 心跳的三大问题

| # | 问题 | 说明 |
|---|------|------|
| 1 | 系统不会思考 | 代码只能写死间隔（白天15分钟/晚上1小时），无法根据对话实时调整——小明很忙时 15 分钟太频繁，很孤单时 15 分钟太冷漠 |
| 2 | 唤醒对话污染上下文 | 每次拍醒的"系统叫醒"话术都留在 AI 上下文里，间隔太短堆积重复垃圾，影响正常对话；间隔太长热聊时又太久不来找 |
| 3 | 机械可预测 | 时间都是定好的（每天 12:00、13:00…），AI 完全被动，用户用久觉得没意思 |

### 2.3 链式机制（核心突破）

> AI 每次被系统拍醒后做两件事：
> 1. **根据之前的对话和时间判断要不要找小明**（心跳的方式）
> 2. **交代系统等下什么时候来拍醒自己**（链式主动关心的核心）
>
> 系统每次拍醒 AI 都要求 AI 告诉自己什么时候再来一趟。
> 对用户：时长是 AI 临时定的，无法预测，聊得火热会更频繁；
> 对 AI：每次苏醒都能确定下次苏醒时间，控制权在自己手里——这才是真正的主动。

### 2.4 回访机制（链式的补充）

> 链式还不够：12:00 触发后 AI 定了 15:00 再唤醒，但 12:30 用户来找 AI 说要去吃饭、开始热聊，15:00 就太久了。
> 于是诞生回访：**AI 每次说话结束后判断，要不要让系统过几分钟再拍醒自己**。
> 用户说去吃饭 → AI 喊系统"半小时后来喊我看看他吃了啥"。

### 2.5 结语

> 做产品不是让 AI 写了就开始宣传的，全是 AI 能想到的东西别人也能做出来，别把市场上早就有的东西重新组合就包装成自己的产品了。

---

## 三、AutoClaw 的讲解：底层概念

### 3.1 LLM 的本质（一切的地基）

**大语言模型（LLM）是"无状态的计算核心"**：
- 不睡觉、不主动、不记得、不等待
- 每次调用都像第一次醒来：输入一段文字 → 输出一段文字 → 这次思考结束
- 下次调用完全不知道刚才发生过什么

所以"AI 主动"必须由**外部系统**（程序）提供三样东西：
- **定时器/事件循环** → 提供"时间感"
- **记忆存储** → 提供"记得"
- **工具执行** → 提供"动手"

```text
外部系统（桌宠程序）—— 提供"时间 / 记忆 / 动手 / 感知"
├─ 定时器 / 事件循环  → 负责"时间"
├─ 记忆存储          → 负责"记得"
├─ 工具执行          → 负责"动手"
└─ 剪贴板 / 界面     → 负责"感知和表达"
        │
        │ 叫醒 / 投喂输入
        ▼
LLM（大脑）—— 只做一件事：输入 → 输出
```

这就是 **Agent（智能体）架构**：LLM 当大脑做决策，程序当身体做执行。

### 3.2 三层机制的本质对照

| 文章叫法 | 本质（学术/工程名） | 调度权在谁 |
|---------|------------------|-----------|
| 心跳 | 定时轮询 polling | 系统（静态配置） |
| 链式 | 自调度 self-scheduling | AI（动态决定） |
| 回访 | 延迟任务 delayed task | AI 决定，系统执行 |

关键区别就一个词：**调度权**。
- 心跳：间隔程序员写死，系统看不到对话，永远无法"看情况"
- 链式：间隔是 LLM 的输出，LLM 看到了对话内容 → 能"看情况"

### 3.3 Function Calling（工具调用）——整个机制的地基

LLM 怎么把"下次 30 分钟叫我"告诉系统？答：**Function Calling**。

```
用户: "我去吃饭了"
        │
        ▼
程序把 [系统设定 + 历史对话 + 工具清单] 打包发给 LLM
        │
        ▼
LLM 返回文字 + 附带一个"意图 JSON":
{
  "tool_call": {
    "name": "schedule_followup",
    "arguments": {"seconds": 1800, "reason": "用户去吃饭了"}
  }
}
        │
        ▼
程序执行对应函数（登记 30 分钟后提醒）→ 结果回填给 LLM
→ LLM 基于结果生成最终回复
```

**核心思想**：LLM 不直接操作世界，它只**输出意图**（结构化 JSON），程序负责翻译和执行。查天气/设提醒/记记忆/安排回访全是同一个模式。

### 3.4 关键工程概念

| 概念 | 说明 | 我们的实践 |
|------|------|-----------|
| 上下文污染 | LLM 输入窗口有限，垃圾消息堆积会让 AI 糊涂 | 唤醒判断用**独立上下文**，只有真正产生的对话才写入历史 |
| 系统提示词 | 程序发给 LLM 的第一段"身份+规则" | 角色名/性格/记忆/待办/行为规则都在里面 |
| 软约束 vs 硬检查 | prompt 规则是软约束，LLM 可能不遵守 | 空闲检测用程序（GetLastInputInfo）硬算，不靠 LLM 自觉 |
| 输出钳制 | LLM 输出不可信，数值必须校验 | next_minutes 强制 10~360 分钟；危险命令拦截 |
| 轻量请求 | 按任务复杂度分配 token 预算 | 唤醒判断/回访生成用 max_tokens 150-200 的小请求 |
| 状态机 | 明确各状态的守卫条件 | 睡眠/贴边/AI忙碌时阻止唤醒 |

---

## 四、学习过程中的问答澄清（理解轨迹）

### 4.1 我的第一次理解（用户提问）

> 链式唤醒 = 还要再走一遍 LLM（程序↔LLM 交互）：说"去吃饭" → LLM 给程序上闹钟 → 到点再问 LLM 要回复什么。
> 回访 = 说"去吃饭" → LLM 直接定时间**并且已经把到点的话输出给程序** → 到点程序直接发预存文本。

**AutoClaw 澄清**：
- 链式理解 ✅ 完全正确：到点后**必走第二次 LLM**（无目标巡查，每次都要重新判断该不该打扰 + 定下次时间）
- 回访理解 ❌ 一个差异：我实现的是**到点再生成**，不是预生成——程序只登记 `{30分钟后, 主题="去吃饭"}`，到点**再问一次 LLM** 生成关心语

**为什么"到点再生成"优于"预生成"**：

| | 预生成 | 到点生成（采用） |
|---|---|---|
| API 调用 | 省一次 | 多一次 |
| 响应速度 | 到点即发 | 慢 1-2 秒 |
| 贴合当时情况 | ❌ 话说死了 | ✅ 能看到到点时的状态，调整语气 |

例：预生成的"回来啦？吃了啥"——到点时发现用户已 2 小时没动电脑，就显得迟钝；到点生成会改成"看你半天没动静，是吃完去忙了吗？"

### 4.2 我的第二次理解（用户提问）

> 链式唤醒 = 程序检测到时间，把当前状态（动没动电脑/有没有其他操作）+ 该判断的事全部发给 LLM，LLM 判断是否来找，下次更智能地生成关心话。
> 回访 = 系统登记了我的事情，到点直接把这个事情发给 LLM 让他以这件事为导向生成回复发过来，**不会判断我的状态**。

**AutoClaw 澄清**（确认 + 两个诚实修正）：
- ✅ 链式完全一致。状态 = 当前时间/星期/空闲分钟数/最近对话
- ✅ 回访一致：prompt 只含事件主题，确实没带状态
- ⚠️ 诚实点 1：回访"不感知状态"是**当时的设计选择**，不是能力限制——可以升级（后来已升级为状态感知）
- ⚠️ 诚实点 2：链式目前只检测"动没动电脑"（空闲时间），**没检测"在干什么"**（前台窗口/正在用哪个程序）——涉及隐私边界，暂不做

### 4.3 最终心智模型（修正版）

```text
链式唤醒 = 定时巡查：状态 + 判断 → 找不找 + 下次几点来
回访     = 事件登记：到点按事件生成关心语 → 直接发（升级后：带状态感知）
```

### 4.4 小蓝（桌宠 AI）自己的理解（2026-08-02）

主人考小蓝"懂没懂链式唤醒和回访机制"，小蓝用自己的话讲了一遍，理解基本准确。作为"AI 视角的总结"原样收录：

**链式唤醒 = "AI 自己给自己定闹钟"**（循环 · 持续陪伴）

```text
1. 程序每 30 秒当一次"闹钟哨兵"，到点就起个后台线程
2. 线程用独立的轻量上下文（不碰主对话）去问 LLM："现在该不该主动找主人？"
3. LLM 返回一段 JSON：{"act":"yes/no", "message":"...", "next_minutes":45}
4. 程序照做：
   - yes → 气泡弹出 message
   - next_minutes → 存成下次唤醒时间（钳制到 10~360 分钟，防止 LLM 说"1年后叫我"）
5. 最妙的是——这轮判断的对话不写入主历史，唤醒一百次，主对话依然干干净净
```

一句话：**循环的、持续陪伴，AI 每次醒来自己决定下次几点醒。**

**回访 = "对话中登记一张未来的便利贴"**（一次性 · 针对具体事件）

```text
1. 对话中说"用户去吃饭了" → LLM 调 schedule_followup(1800, "用户去吃饭了")
2. 程序把 {时间, 内容, 类型} 塞进提醒队列
3. 程序每秒轮询队列，到点才触发
4. 触发时再起线程让 LLM 生成一句关心话 → 气泡显示
```

一句话：**一次性的、针对具体事件，登记完就等着，到点办一次就完事。**

**两者对比（小蓝版）**：

| | 链式唤醒 | 回访 |
|---|---|---|
| 谁定时间 | LLM（每次动态决定） | LLM（对话中一次决定） |
| 触发方式 | 30秒轮询 → 问一次 | 每秒轮询队列 → 到点触发 |
| 生命周期 | 循环，无终点 | 一次性，到点就完 |
| 共同本质 | 程序当身体定时，LLM 当大脑判断，AI 只输出意图 JSON | 同左 |

**小蓝的总结**：链式唤醒是"AI 决定自己持续存在的节奏"，回访是"AI 在对话中埋一个未来的种子"。两条路最终都通向同一个答案——用程序的定时器给无状态的 LLM 装上"时间感"。

**AutoClaw 点评**：理解准确 ✅。补充一点——回访在 v6.18 升级后还带**状态感知**（到点时 LLM 能看到空闲分钟数/最近对话），所以回访触发时的"生成"不是死板的，会看情况调整语气。

---

## 五、桌宠实现落地（v6.18 主动关心系统）

### 5.1 架构总览

```
💗 主动关心（右键 → 互动 → 开关）
│
├─ 链式唤醒（AI 自主调度）
│    到点 → 轻量 AI 判断（独立上下文，不污染主对话）
│    ├─ 空闲>30分钟 / 深夜 / 忙 → 不打扰（act:no）
│    ├─ 有值得关心的事 → 生成关心语冒泡（act:yes）
│    └─ AI 自己定下次唤醒间隔（钳制 10~360 分钟）
│
├─ 回访机制（对话中安排）
│    你："我去吃饭了" → AI 调 schedule_followup
│    → 到点 AI 主动（带状态感知）："回来啦？吃得怎么样？"
│
└─ 兜底：AI 不可用 → 随机台词心跳（原行为）
```

### 5.2 链式唤醒数据流（伪代码）

```
[程序] 每 30 秒检查 _check_active_chat():
    到点了？→ 是 → 起后台线程 _wakeup_worker()
[线程] 轻量判断请求 _ai_wakeup_judge():
    prompt = "现在是 15:26（周日），电脑空闲 3 分钟，
              最近对话：用户说去吃饭了。
              判断：要不要主动找用户？
              规则：空闲>30分钟/深夜/忙 → 不打扰。
              只返回 JSON：{"act":"yes/no","message":"...","next_minutes":30}"
[LLM] 返回 {"act":"yes","message":"回来啦？吃了啥","next_minutes":45}
[程序] ① act=yes → 气泡显示 message
       ② next_minutes=45 → 存为下次唤醒（钳制 10~360 分钟）
       ③ 判断对话【不写入】主对话历史 ← 防污染关键
```

### 5.3 回访数据流（伪代码）

```
[对话中] LLM 调 schedule_followup(1800, "用户去吃饭了")
[程序]   提醒队列 += {时间: now+1800, 内容: "用户去吃饭了", 类型: followup}
[程序]   每秒轮询提醒队列 → 到期 → 起线程 _ai_followup():
         prompt = "现在是 16:05（周日），用户已空闲 3 分钟，
                   最近对话：…
                   用户之前提到：用户去吃饭了。按约定主动关心一下。
                   空闲>60分钟→体谅；<10分钟→亲近。"
[LLM]   生成"小蓝掐着点回来啦～主人吃饱喝足了吗？🐟" → 显示
```

### 5.4 两种机制的精确区别

- **链式唤醒**：闹钟是 LLM 定的，到点后 LLM 要**重新判断要不要来**（不确定的巡查）
- **回访**：闹钟是 LLM 定的，到点后 LLM **不用判断要不要来**（约定已定），**只生成怎么说**（且带状态感知）

共同点：都要在到点时走一次 LLM——"当时该说什么"只有当时的 LLM 知道。

### 5.5 实测结果（真实 API）

| 测试 | 结果 |
|------|------|
| 链式唤醒判断 | AI 返回 `{act:no, next_minutes:30}` —— 判断不打扰，自定 30 分钟 ✅ |
| 间隔钳制 | 10-360 分钟强制范围 ✅ |
| 回访触发 | 安排 2 秒回访 → "回来啦？吃得怎么样，有没有尝到特别好吃的菜？" ✅ |
| 回访状态感知 | 空闲短 → "小蓝掐着点回来啦～主人吃饱喝足了吗？🐟" ✅ |
| schedule_followup 工具 | ✅ |
| 用户活跃检测 | GetLastInputInfo 空闲分钟数 ✅ |

---

## 六、概念对照总表

| 文章概念 | 计算机本质 | 桌宠实现位置 | 一句话 |
|---------|-----------|------------|--------|
| 心跳 | 定时轮询 | `_check_active_chat` 30 秒检查 | 系统到点叫 AI |
| 链式 | 自调度 | `_ai_wakeup_judge` 输出 next_minutes | AI 自己定下次叫醒时间 |
| 回访 | 延迟任务 | `schedule_followup` + 提醒队列 | 对话中登记未来动作 |
| — | Function Calling | 15+ 个工具 | LLM 输出意图，程序执行 |
| — | 上下文隔离 | 判断请求独立上下文 | 唤醒不污染主对话 |
| — | 输出校验 | 间隔钳制/危险拦截 | LLM 输出不可信 |
| — | 软约束 vs 硬检查 | prompt 规则 + 程序检测 | 双保险 |
| — | 状态感知（我们的增强） | 回访 prompt 带时间/空闲/最近对话 | 比文章方案多一层 |

---

## 七、一句话总结

> 怎么让一个无状态、被动响应的 LLM，表现出有时间感、会主动关心的行为？
> 答案：**把"时间感"外包给程序（定时器），把"判断力"留给 LLM（每次唤醒问一次），再用 function calling 把 AI 的意图翻译成系统的行动。**
> 这套模式不止用于桌宠——所有 agent（自动回复、监控机器人、定时报告）都是这个骨架。

---

## 八、未来可扩展方向（未做）

- [ ] 前台窗口感知：检测用户正在用哪个程序（GetForegroundWindow），判断忙/闲更准 —— 涉及隐私边界，需用户确认
- [ ] 唤醒判断的情绪维度：结合记忆中的用户心情/作息做更细的打扰决策
- [ ] 链式间隔的强化学习：根据用户对主动消息的回应率动态调整 AI 的默认间隔偏好
- [ ] 主动消息的"不打扰模式"：用户忙碌时自动顺延，而不是只跳过本次

---

*文档整理：AutoClaw · 2026-08-02*
*对应桌宠版本：v6.18（链式 + 回访 + 状态感知）*

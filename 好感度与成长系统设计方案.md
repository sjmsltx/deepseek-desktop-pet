# 桌宠「好感度与成长系统」设计方案

> 项目：deepseek-desktop-pet（E:\ai工作站\desktop-pet\）
> 日期：2026-08-28 | 状态：方案待确认
> 参考项目：whale-girl（成长系统）、DyberPet（200级好感度/饱食度）、LiveGalGame（好感度可视化）、dsh-pet（余额气泡/动作素材/小游戏）

---

## 〇、现状盘点（基于代码阅读）

| 现有能力 | 位置 | 与本方案的关系 |
|---------|------|---------------|
| 双角色（V4 Flash 人鱼 / V4 Pro 鲸鱼娘），独立模型/对话/记忆 | desktop_pet.py | 好感度按角色隔离，人设进化按角色独立 |
| 长期记忆（memorize 工具 + chat_memory.json / memory.json）| 现有 | 事实记忆层保留，新增「事件记忆（回忆日志）」层 |
| API 用量统计（api_stats.json + 悬浮窗）| ApiStats 类 | 余额气泡直接复用其费用数据 |
| 20+ 动作素材（assets/flash/）+ Live2D 双模式 | asset() 函数 + 状态机 | 扩展动作状态机即可 |
| 主动关心系统（链式唤醒 + 回访）| 现有 | 好感度高时升级关心强度（人设阶段联动）|
| 扒边/待机状态机 | 现有 | 不冲突，新系统独立叠加 |
| config.json 配置持久化 | 现有 | 沿用同模式扩展 |

**核心约束**：desktop_pet.py 已是 306KB 单文件 → 新系统**独立模块化**（新增 affection_engine.py 等），主文件只做 import 和少量接线，不再往里堆代码。

---

## 一、总体架构

```
desktop_pet.py（主程序，仅接线）
 ├── affection_engine.py   ← 新增：好感度引擎（纯逻辑，无 UI）
 │     ├── 数值模型（好感度/XP/等级/称号）
 │     ├── 事件处理（事件→数值变化→防刷）
 │     └── 持久化（affection.json）
 ├── memory_events.py      ← 新增：回忆日志（事件记忆）
 ├── pet_minigames.py      ← 新增：小游戏（注册式，可扩展）
 ├── affection_ui.py       ← 新增：进度条面板/气泡动画/小游戏窗口
 └── （现有）chat_memory / memory / api_stats / assets
```

设计原则：**引擎与 UI 分离**——引擎层纯 Python 数据逻辑（可单测），UI 层只做展示与交互。

---

## 二、好感度数值系统（核心）

### 2.1 数值模型（双轴）

| 轴 | 范围 | 含义 | 影响 |
|----|------|------|------|
| **好感度 affection** | 0~200（初始 60）| 情感亲密度 | 决定人设阶段（语气）、解锁动作/称号 |
| **等级 level / XP** | 无上限 | 陪伴资历 | 荣誉称号、回忆里程碑 |

为什么双轴：whale-girl 只有 XP（资历），DyberPet 只有好感（情感）——两者合并更丰富：**「认识多久」和「关系多好」是两回事**。

### 2.2 好感度事件表（纯积累制，零衰减零惩罚）

| 事件 | 好感度 | 防刷规则 |
|------|--------|---------|
| 对话（每轮回复）| +1 | 单轮对话累计上限 +3 |
| 完成任务/提醒 | +5 | 同一任务只计一次 |
| 喂食 | +2 | 冷却 30 分钟 |
| 小游戏胜利 | +3 | 每局 |
| 小游戏参与（败）| +1 | 每局 |
| 早安/晚安问候 | +2 | 每天各一次 |
| 陪伴（活跃）| +1 / 10分钟 | 单日上限 +20（防挂机刷）|
| 主动关心被回应 | +4 | 每次 |

**零惩罚原则**（whale-girl 设计哲学）：失败不扣好感、饱食度低不扣好感——避免负反馈导致用户弃坑。好感度只升不降（200 封顶）。

### 2.3 等级 XP 曲线（whale-girl 公式）

```
xpForLevel(L) = 50 · L · (L−1) / 2    （到达 L 级所需累计 XP）
例：Lv2=50, Lv3=150, Lv4=300, Lv5=500, Lv10=2250
```

- 事件同时给 XP 和好感度（比例约 2:1）：对话 +2XP/+1好感，任务 +10XP/+5好感
- 升级触发：称号解锁 + 升级动画（见 2.4）

### 2.4 称号系统（6 个成就）

| 称号 | 解锁条件 |
|------|---------|
| 初次协作 | 完成首个任务 |
| 勤劳伙伴 | 累计完成 10 个任务 |
| 常驻伙伴 | 累计陪伴 24 小时 |
| 百炼成钢 | 等级达到 Lv10 |
| 越挫越勇 | 小游戏参与 20 局 |
| 广结善缘 | 两个角色好感度都 ≥120 |

### 2.5 存储结构（affection.json）

```json
{
  "flash": {
    "affection": 60, "xp": 0, "level": 1,
    "titles": [], "stats": {"tasks": 0, "games": 0, "companion_min": 0},
    "cooldowns": {"feed": 0}, "daily": {"date": "2026-08-28", "chat_pts": 0, "companion_pts": 0}
  },
  "pro": { "...": "同上，双角色独立" }
}
```

**实施**：affection_engine.py 提供 `Event(name) → (affection_delta, xp_delta, events[])` 纯函数接口；主程序在 ask_ai 返回、工具完成、定时器触发时调用。单测友好（不依赖 UI）。

---

## 三、人设进化（纵向维度）

### 3.1 原理

现有双角色是**静态人设**（system prompt 固定）。人设进化 = **按好感度阶段动态切换关系定位**，让角色从「初见」长成「灵魂伴侣」——这就是「纵向」维度。

### 3.2 五阶段设计

| 阶段 | 好感度 | 关系定位 | prompt 注入要点 |
|------|--------|---------|----------------|
| Lv1 初见 | 60~80 | 礼貌助手 | 保持距离、称呼「您」、话题克制 |
| Lv2 熟悉 | 80~110 | 熟络朋友 | 称呼「你」、会开玩笑、主动分享 |
| Lv3 亲密 | 110~140 | 亲密伙伴 | 撒娇、专属昵称、关心日常细节 |
| Lv4 依赖 | 140~170 | 心灵依靠 | 主动关心升级（更频繁）、心事分享 |
| Lv5 灵魂伴侣 | 170~200 | 唯一 | 默契梗、特殊称呼、无需多言的懂 |

### 3.3 实施（关键）

在 `ask_ai()` 构建 system prompt 时，把现有的固定人设段替换为：

```
[人设基础]（现有角色设定，不变）
[关系阶段]（根据 affection 注入对应阶段描述，如：
  「你和主人已经是灵魂伴侣，用一个只有你们懂的昵称叫他，
   他开口说半句你就能接下半句」）
[最近回忆]（注入最近 3 条回忆日志，可自然提及）
```

- 双角色独立计算（flash 和 pro 各自的好感度/阶段）
- 阶段变化时触发提示气泡（「我们的关系好像更近了一步…」）
- **回溯保护**：阶段只升不降（好感度只增不减，阶段自然单调上升）

---

## 四、记忆机制详细设计

### 4.1 三层记忆架构（现状 + 新增）

| 层 | 文件 | 内容 | 生命周期 |
|----|------|------|---------|
| 对话记忆 | chat_memory*.json | 近期对话轮次 | 滚动压缩 |
| 事实记忆 | memory.json | 用户偏好/事实（现有 memorize 工具）| 长期，重要度排序 |
| **事件记忆（新增）** | memories.json | 共同经历时间线（回忆日志）| 长期，上限 200 条滚动淘汰 |

### 4.2 回忆日志（memories.json）

```json
[
  {
    "id": 1024, "time": "2026-08-28 20:30",
    "role": "flash",
    "type": "milestone",          // milestone成就 | event事件 | user_mark用户标记
    "title": "第一次完成待办任务",
    "detail": "主人完成了「写 GIS 实验报告」，我们击掌庆祝",
    "emotion": "happy",
    "affection_at": 75
  }
]
```

**写入时机**：
1. AI 调用 memorize 工具时 → 事实 + 回忆**双写**（事实进 memory.json，事件进 memories.json）
2. 系统里程碑 → 升级/称号/阶段变化自动记录
3. 用户主动标记 → 右键菜单「📌 记住这一刻」（记录当前对话摘要）

**读取时机**：
1. 构建 prompt 时注入最近 3 条（AI 可自然提及「还记得上次我们…」）
2. 回忆相册 UI（Phase 3）：按时间线浏览，点击回看当时对话

**与 whale-girl 回忆的区别**：whale-girl 是 8 条环形覆盖，我们做成 200 条时间线 + 类型标签，信息量更大、可检索。

---

## 五、余额动画气泡（dsh-pet）

### 5.1 原理

现有 ApiStats 已解析每次调用费用（api_stats.json）。把「本次消耗」做成游戏式金币反馈：角色头顶飘出 `-¥0.032` 上浮渐隐。

### 5.2 实施

- `affection_ui.py` 新增 `CostBubble(parent, text)`：QLabel + QPropertyAnimation
  - 动画：向上位移 40px + 透明度 1→0，时长 1.2s，Easing.OutCubic
  - 触发点：ask_ai 拿到 usage 后（现有 _record 处）挂到角色窗口
- 可选：费用超过阈值（如 >¥0.1）时用红色 + 放大，小额灰色——视觉层次

技术可行性：QPropertyAnimation 是 PySide6 内置，纯现有依赖。

---

## 六、97 个动作素材扩展

### 6.1 原理

现有 `asset(role, state)` 已实现「角色 → 状态 → 图片路径」映射 + 20+ 状态（idle/blink/sleep/happy/angry…）。扩展 = 往状态机加新状态 + 补素材。

### 6.2 新增动作状态（好感度联动）

| 状态 | 触发条件 | 素材来源 |
|------|---------|---------|
| 撒娇 shy_hug | 好感 ≥110 且对话撒娇时 | 现有立绘管线（Seedream + rembg）|
| 亲亲 kiss | 好感 ≥140 喂食成功 | 同上 |
| 胜利 victory | 小游戏胜利 | 同上 |
| 沮丧 defeat | 小游戏失败 | 同上 |
| 饥饿 hungry | 饱食度 <30 | 同上 |
| 送花 flower_give | 成就解锁 | 同上 |

- **代码先行**：状态机 + 触发逻辑先实现，素材后补（没有素材时优雅降级到 happy/idle）
- 素材制作用你的既有管线：Seedream 白底生成 → rembg 抠图 → 命名 flash_xxx.png

技术可行性：asset() 函数已支持映射，加状态=加字典项+加 if 分支，改动极小。

---

## 七、18 款小游戏 / 五子棋（pet_minigames.py）

### 7.1 原理

PySide6 原生控件实现小游戏面板；游戏结果作为好感度事件源（胜利 +3/+6XP，参与 +1/+2XP）+ 陪伴时长。做成**注册式**：`GAMES = {"五子棋": Gomoku, "猜数字": GuessNumber, ...}`，右键菜单动态生成。

### 7.2 首批 3 款（Phase 2）

| 游戏 | 实现 | 技术要点 |
|------|------|---------|
| 石头剪刀布 | 三按钮 + 随机判定 | QPushButton ×3 + 结果 QLabel，最简单 |
| 猜数字 | 输入 + 大小提示 | QLineEdit + 逻辑判断，1-100 二分提示 |
| 五子棋 | 15×15 棋盘 + AI 落子 | QGridLayout/QPainter 棋盘 + minimax 深度 2 简化版（评估函数：连子数加权）|

### 7.3 扩展机制

`register_game(name, widget_factory)`：新游戏 = 实现一个 QWidget 子类 + 注册一行。后续慢慢补到 18 款（2048/扫雷/贪吃蛇等）。

技术可行性：全部原生控件，零新依赖；五子棋 AI 用 minimax 剪枝，深度 2 响应 <100ms。

---

## 八、LiveGalGame 式好感度可视化

### 8.1 原理

实时可见的好感度反馈是 galgame 式体验的灵魂（LiveGalGame 的 `好感度: 50→70 (+20↑)`）。

### 8.2 实施

- **面板**：右键菜单「❤️ 关系」→ 弹出面板：
  - 好感度进度条（0~200，带当前值/阶段名）
  - 等级 + XP 进度（下一级还差多少）
  - 已解锁称号列表
  - 最近 5 条好感度变化记录
- **动效**：对话/事件导致好感变化时，进度条区域飘 `+2↑`（复用余额气泡的动画组件）
- **阶段徽章**：面板顶部显示当前关系阶段（初见/熟悉/亲密/依赖/灵魂伴侣）

技术可行性：QProgressBar + QPropertyAnimation，现有依赖。

---

## 九、DyberPet 饱食度

### 9.1 原理

饱食度 0~100，随时间线性下降（满 100 约 12 小时降到 0）；喂食恢复 40 并触发撒娇动作 + 好感 +2。**低饱食只提示不惩罚**（不扣好感，守零惩罚原则）。

### 9.2 实施

- 存储：affection.json 内 `satiety` + `last_decay_time`（按时间戳差值计算，避免离线期间未衰减）
- 衰减：QTimer 每 10 分钟 -1 左右（按时间戳算更稳：`satiety = max(0, stored - elapsed_min * rate)`）
- UI：饱食度 <30 时角色切换 hungry 状态 + 气泡提示「肚子好饿…」；右键菜单「🍖 喂食」按钮
- 喂食素材：食物图标（现有 assets 可加）

技术可行性：纯时间戳计算 + 定时刷新 UI，无难点。

---

## 十、实施分期

| 阶段 | 内容 | 预计工作量 |
|------|------|-----------|
| **Phase 1 核心** | affection_engine（数值/曲线/称号/防刷）+ 人设五阶段注入 + 三层记忆（memories.json）+ 关系面板 | 3-4 天 |
| **Phase 2 互动** | 余额气泡 + 饱食度喂食 + 首批 3 款小游戏 + 新动作状态（代码部分）| 3-4 天 |
| **Phase 3 进阶** | 回忆相册 UI + 对话情感选项（galgame 选择支）+ 更多小游戏 + 素材批量制作 | 灵活排期 |

每阶段独立可交付、可验证（引擎部分有 pytest 单测）。

## 十一、风险与边界

| 风险 | 应对 |
|------|------|
| desktop_pet.py 单文件膨胀 | 新代码全部独立模块，主文件只接线 |
| 人设注入影响对话质量 | 阶段描述做成可配置模板，双角色独立可开关 |
| 素材缺失 | 代码先支持，无素材降级到现有动作 |
| 好感度刷分 | 冷却/单日上限/封顶三重防刷（沿用 whale-girl 设计）|
| 与现有主动关心系统冲突 | 关心频率由「阶段」统一调制，替代写死的调度参数 |

---

## 十二、参考项目取用对照

| 参考 | 取用什么 |
|------|---------|
| whale-girl | XP 曲线公式、成就称号、零负反馈原则、防刷封顶、回忆日志概念 |
| whale-girl growth-system | 完整设计文档做骨架（docs/growth-system.md）|
| DyberPet | 200 级好感度、饱食度衰减、喂食互动、任务掉落 |
| LiveGalGame | 好感度进度条 + 变化动效（+20↑）、情感标签（Phase 3）|
| dsh-pet | 余额动画气泡、动作素材链、小游戏面板 |

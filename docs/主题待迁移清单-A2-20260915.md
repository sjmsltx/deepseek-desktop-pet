# 主题 token 化 A2 · 迁移清单（2026-09-15 · **已完成**）

> ✅ **A2 已收口：产品代码硬编码颜色 = 0 处**。本文件保留为**轨迹记录**（当初的逐处清单已全部清掉）。

## 轨迹

| 阶段 | 范围 | 残留 |
|------|------|------|
| 起点 | A1 之前（主题只覆盖聊天面板+气泡） | 172 处 / 11 文件 |
| **A1** | 唯一 token 源 `pet_theme.py` + 设置窗口并入 + 提示词诚实化 + 护栏（ratchet） | 144 处 |
| **A2-1** | 15 款小游戏（模块级主题快照 + `T()` + 样式工厂 + 色板 token 映射） | 89 处 |
| **A2-2** | 主程序 / 卡片 / 关系面板 + **生效主题中央通道**（`set_active`/`subscribe`）+ 护栏收紧为 0 | **0 处** |

## 最终状态

| 项目 | 值 |
|------|-----|
| token 总数 | **86**（`panel` / `bubble` / `dialog` / `minigame` / `host` 五个分组） |
| 产品代码硬编码颜色 | **0**（只剩显式 `# theme-exempt` 的档案数据 / prompt 示例 / 护栏正则） |
| 主题生效路径 | 改 token → `pet_theme.set_active()` → 所有订阅模块自动重刷（面板/气泡/历史消息/卡片/已开小游戏/已开面板） |
| 持久化 | 主题名写 `config.json`；启动时 `_load_ai_config` 记住 → `_init_finish` 恢复（早于历史回显） |
| 护栏 | `tests/test_theme_tokens.py`（唯一源 / token 表 / 设置窗零硬编码 / prompt 口径 / **产品代码必须为 0**） |

## 复核命令

```powershell
python -m pytest tests/ -q           # 93 项
python regression_test.py            # 40 项
python tests/golden_ui.py check      # 黄金基线 7 分区
python tests/test_theme_tokens.py list   # 应为「共 0 处」
```

## 后续可选项（未做，非缺陷）

1. **流式期间的思考区/状态行**：创建时取色，切主题要等新消息才完全刷新；
2. **历史消息里的 HTML 内联色**（`chat_render` 生成的代码块/表格）：渲染时快照，老消息不随主题变；
3. **B 方案（皮肤包）** 与 **C′（数据层热重载）**：待评估，不在本轮范围。

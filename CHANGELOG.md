# Changelog

本项目所有重要变更记录于此文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.9.0] - 2026-09-20

> 发版节奏：使用者定的“不逐个小批次发版，攒到大节点再发” —— 本版就是那个节点：
> **技能包生态 + MCP 接入与治理 + 平台抽象层地基**一次到位（内部迭代号 v6.42→v6.73）。

### 新增

- **旧回归用例与 UI 基线跟上重构（v6.73，webchat 侧接手）**
  - `regression_test.py` 的 **H21** 原本断言“设置窗 6 分类且名字固定”——本批已拆 8 类/10 页，
    遂重写为**十分类**（与 `settings_ui.PAGES` 交叉断言 + 每页行数 ≤ 14）；全套回归 **40/40 PASS**
  - `tests/golden/ui_baseline.json` **重采主题基线**：原基线（09-15）是 `theme_blue_white`，
    而使用者配置现为 `theme: default`（`pet_theme.py` 本批未改）→ 属环境/配置变化，非代码退化；
    重采后 `golden_ui.py check` 完全一致，且**基线文件 git diff 仅 2 行**
    （只有 `current_theme` 与 `theme_hash`），证明其它 6 个分区零飘移

- **P1 平台抽象层 · 批次 1（v6.72）**：新增 `platform_layer.py` —— 把与操作系统耦合的能力收成一组接口：
  - **能力清单（11 项）**：`open_path` / `open_url` / `clipboard` / `foreground` / `ocr` / `tts` / `asr` /
    `hotkey` / `beep` / `autostart` / `window_effects`；`capabilities()` + `report()` 如实回答
    “这台机器能不能、经谁实现”。
  - **先转发、后收编**：已收编项直接转发既有实现（`pet_sysutils` / `pet_foreground` / `pet_docs` /
    `voice_io` / `asr`）；尚未迁走的（**开机自启**、窗口特效）经 `bind_host(宿主)` 转发——
    没绑定时**如实报不可用**，不假装支持，也**不另写一套**（自启现有方案是**启动文件夹快捷方式**，
    不是注册表 Run 项，已在文档里钉住防迁移时改错）。
  - **跨平台前提**：导入期不 import 平台专有模块、能力缺失不抛异常（测试用**独立子进程查增量**验证）。
  - 本批**零修改现有文件**（对方 Batch 3 仍在制品，避免撞车）；迁移顺序（批次 2–5）见
    `docs/平台抽象层-20260920.md`；新增 `tests/test_platform_layer.py`（16 项）。


- **P1 平台抽象层 · 批次 2（v6.72）：自启与窗口特效收编 + 修「自启只能关不能开」（v6.72）**
  - **收编**：`platform_layer` 新增 `autostart_enabled/enable/disable/set/toggle`、`startup_dir/autostart_path/autostart_target`、`create_autostart_lnk`、`_cleanup_legacy`、`apply_pet_window`、`set_topmost`、`clear_dwm_shadow`；宿主 `desktop_pet.py` 的
    `is_autostart_enabled/toggle_autostart` 改成薄封装（删掉 4 个私有方法），主窗标志改为
    `pl.apply_pet_window(self)`、运行时置顶改为 `pl.set_topmost(win, True)` —— **等值搬迁，行为不变**。
  - **顺带修一个真 bug**：原 `toggle_autostart` 删完文件后写 `if True: … else: …`，恒真 →
    **「关闭」永远执行、真正打开自启的代码是死代码** → 面板上勾选也开不了自启（关掉后就再也开不回来）。
    现改为显式 `autostart_set(on)` / `autostart_toggle()`（返回新状态），语义清楚且可测。
  - **测试**：`tests/test_platform_layer.py` 16 → **23 项**（新增自启开关双向生效、旧启动项清理、开发版/打包版目标、非 Windows 降级、窗口标志真建 QWidget 验证）；
    **用临时目录 + 注入，绝不碰使用者真实的启动文件夹**。全量 **528 passed / 8 skipped**；
    `regression_test.py` **40/40**（H21 已随本批改为十分类）。

- **P1 平台抽象层 · 批次 3（v6.73）：调用方全部切换，并加边界守护测试**
  - **切换**：`desktop_pet.py` / `settings_ui.py` / `tools_executor.py` 里所有直连平台接口的调用
    改走 `platform_layer`（打开文件/URL/搜索、剪贴板、OCR、前台进程、提示音、热键注册与注销、
    锁屏、DPI 声明），共 **30+ 处**；**删掉宿主里重复的热键过滤器实现**（与 `pet_sysutils` 那份等价，
    宿主那份因定义在后一直覆盖 import）；清掉因此变成未使用的导入（`fgwin`、`winsound` 等）。
  - **抽象层新增**：`lock_screen` / `enable_dpi_awareness` / `foreground_categorize|busy_level|label|privacy_note`；
    `beep(kind)` 词表对齐宿主原序列（msg=880+1320、remind=660+990、其它=660）→ 能力项 **11 → 13**。
  - **新增边界守护测试** `tests/test_platform_boundaries.py`（7 项）：调用方文件里**不得**再出现
    `os.startfile(` / `winsound.` / `ctypes.windll.user32.` / `fgwin.` / 那些旧 helper 的直连调用
    （有意豁免需在行尾写 `# platform-exempt` 并说明理由）——把"没有旁路"钉成测试。
  - **连带修**：`tests/test_api_options.py` 的 OCR 打桩点从 `dp.ocr_image` 改到 `pl.ocr_image`（调用口已迁移）。
  - 结果：全量 **539 passed / 8 skipped / 退出码 0**；`regression_test.py` **40/40**；golden **完全一致**。

- **P1 平台抽象层 · 批次 5（v6.74）：设置窗「系统」页加只读平台能力区块**
  - 把 `platform_layer.report()` 的 **13 项能力**（经谁实现 / 是否可用）渲染成只读文本块，
    带「🔄 重新检测」按钮；打开设置与刷新时自动重采。文本可选中复制，方便排查时贴给我们。
  - **刻意不新增导航页**：本批已把设置拆成 10 个分类，`regression_test.py` 的 H21 会断言
    "正好 10 分类 + 固定名字"，新增第 11 页会把那条测试弄红 → 改为在「系统」页内追加区块（不动导航结构）。
  - 探测异常（`report()` 抛错）只在区块里显示「能力检测失败：…」，不把设置窗带崩。
  - 测试：新增 `tests/test_settings_platform_page.py`（6 项：仍是 10 页 / 区块渲染 13 项 /
    重新检测按钮生效 / `_refresh()` 会重采 / 只读 / 失败不抛）；另出**截图交视觉模型确认**
    （导航 10 分类、无乱码截断、13 项全勾）。
  - 结果：全量 **549 passed / 8 skipped / 退出码 0**；回归 **40/40**；golden **完全一致**。
### 修复

- **权限闸门漏了 `open()`：零权限技能包能读密钥、覆盖文件（v6.71，端到端验证发现）**：
  - **根因**：静态扫描只认**关键词**（`subprocess` / `os.remove` / `requests.*` …），
    **`open()` 根本不在判据里** —— 10 类权限里的 `files.read` / `files.write` 因此**没有对应的强制点**。
  - **影响（PoC 实测）**：声明零权限的包 ① 安装即启用、无需任何确认；② 能读程序目录 `config.json`
    （里面有使用者的 API key）；③ 能往程序目录写文件（等于能覆盖 `config.json` 或程序自己的代码）。
    出网仍被“先声明 + 白名单”挡住 → 属**本地影响**（窥密钥 + 覆盖文件）。
  - **修复**：新增 AST 级判据 `skill_pack.file_api_permissions()` —— `open()` 按 mode 分读/写，
    `os.remove/makedirs/replace`、`shutil.copy/move`、`Path.read_text/write_text`、
    常见 pandas/PIL 读写方法都算；**写入必须声明 `files.write`**，只读时声明 `files.read` 或 `files.write`
    均可；**不收 `replace`/`rename`/`chmod` 方法名**（`str.replace()`、`DataFrame.rename()` 会同名误报）。
    v1 老插件（`manifest_version=1`）仍只拦永禁，不受影响。
  - **连带**：`plugins/vision/plugin.json` 补 `files.read` 声明（它本来就 `open(path, 'rb')` 读图）；
    其余 4 个官方包已声明齐全，零影响；7 个 v1 主题/skill 插件不受影响。
  - **测试**：新增 `tests/test_file_api_permissions.py`（43 项，含「接收者是表达式」的回归点与不误报用例）。
- **Batch 3 端到端真机验证（2026-09-20）**：MCP 桥接改接**第三方真实 server**
  （npm 官方 `@modelcontextprotocol/server-filesystem`）→ 连接 / 14 个工具 / inputSchema / 读写分类 /
  真读真写落盘 / 审计联动 / 断开无残留，共 15 项全过；技能包 6 类恶意形态全拒；
  治理三件真生效（额度真计数、白名单真拦 http 请求、审计真落盘并按 mtime 清理）。
  固化为 `tests/test_batch3_e2e.py`（18 项；对第三方 server 那份需 `RUN_SLOW_MCP=1`）。
- **测试“假绿”治理（v6.70，跳会话接手时发现）**：
  - **根因**：离屏平台默认加载 **0 个字体** → 所有文字零宽零高 → 任何“内容装得下 / 按像素省略 /
    高度够不够”的断言都会**假通过**（`test_stats_window` 那组在无字体口径下 6/6 绿，开字体后立刻 1 失败）。
    影响 **7 个测试文件、40+ 处字体度量断言**（含 golden 指纹）。
  - **修复**：新增 `tests/conftest.py` 统一设 `QT_QPA_FONTDIR`（仅当本机目录存在）→ 所有 UI 测试
    跑在**真实字体口径**；随之暴露并修掉「API 统计悬浮窗最胖内容装不下」的真问题：抽出 `_fit_window()`，
    按布局 `minimumSize / sizeHint` 自适应宽高，且**每次刷新后重新贴合**（余额/来源行变胖不再挤压；
    原先取的是刷新前的估值，且只在开窗时算一次）。
  - **COM 致命异常隔离**：`office_doc info` 的探测会**把进程打崩**（Windows 致命异常
    `0x800706ba/b`，Python `try/except` **抓不住**）→ 探测改到独立子进程（新增 `office_probe.ps1`；
    用 PowerShell 而非 `sys.executable -c`，避免打包后变成再启一个桌宠）；三个真打 COM 的用例改为
    **子进程隔离执行**（`tests/_office_child.py`）；`conftest` 增加会话后**清理无窗口办公进程**
    （实测曾残留 3 个 `wps` 孤儿，对应 HANDOFF 坑 #8）。
  - 结果：字体口径全量 **452 passed / 退出码 0 / 无致命异常**（v6.71 加入文件 API 与 E2E 两套后为 **505 passed / 8 skipped**）。
- **右侧信息栏折叠后变成一整块空面板（v6.66，使用者实测反馈）**：
  - 根因一：折叠只 `setVisible(False)` 隐藏内部控件，**没改面板宽度** —— QFrame 仍然
    `setFixedWidth(186)`，于是留下一块 186px 的空白面板盖住聊天与输入框 → 现在折叠
    **真的收到 24px**（只留把手），展开恢复原宽
  - 根因二：面板底色用的是主题弹层色（**半透明，alpha 0.5**），后面的聊天文字透出来，
    看起来像标题串了内容 → 新增 `_solid()` 把主题色换算成不透明色（**不写颜色字面量**，
    守“硬编码颜色只减不增”护栏）
  - 页签文字改为**只放图标** + 完整名字进悬浮提示（186px 里放不下四个字，会被省略成「…」）；
    **图标必须是细窄单色符号**（`☰ ◎ ¥ ✓ ✎ ⌗`，实测 6–12px）——一开始用了彩色 emoji
    （📋/📊/📝 各 16px）反而被省略回「…」，见下方“修复”里的详细根因；**自定义页签名保留不截断**
  - 任务列表为空时显示「（暂无任务）发消息后若还在忙，就会排在这里」，不再留一大块空白
- **删掉旧版遗留的「常驻任务把手」（v6.66）**：chat 与信息栏之间那个 22×40 的 📋 方块与
  面板自带的 ◀/▶ 按钮功能重叠，使用者反馈“折叠后多出一个像任务栏的按钮” → 已整块移除，
  折叠/展开统一用面板上的箭头按钮。
- **信息栏文本页停在占位符、不刷新（v6.66）**：`状态/花费/系统` 页本来只在 2 秒定时器里刷新，
  折叠启动后再展开会看着“一直是 …” → `refresh_dynamic(force=True)`，**展开、切页签、窗口刚显示**
  都立即填内容；占位符由 `…` 改为「正在读取…」。
- **页签能重复添加（v6.66）**：`状态/花费/待办/系统` 是同一份全局信息，加两个只是重复显示 →
  现在**除便签外同类型只允许一个**（已存在就切过去并提示原因）；加载配置时也会把已有的重复页签洗净。
- **点页签从来不切内容区（v6.66，本轮真凶）**：`_on_tab_changed()` 只改了标题与底部说明，
  **从未调用 `stack.setCurrentIndex()`** —— 点「◎ 状态」时标题写着“状态”、底下一行也是状态的说明，
  但内容区永远停在任务页（没任务时就是一块空白）→ 使用者看到“内容一片空”。已补上切页。
- **页签显示成「…」（v6.66）**：页签图标用了彩色 emoji（📋/📊/📝 真实字体各 16px）
  + 页签内边距刚好超过页签格宽（26px，可用文字宽≈16px）→ Qt `ElideRight` 直接把它换成省略号
  （只有 12px 的 ✔ 幸免 —— 这也正是使用者截图里“✔ 正常、其余是 …”的原因）。改用细窄单色符号
  `☰ ◎ ¥ ✓ ✎ ⌗`（实测 6–12px），并给页签栏收紧内边距 + 保底宽 `min-width:14px`。

### 新增

- **技能包系统（v6.67，Batch 3-1）**：在现有插件系统上补【权限声明 / 打包校验 / 可管理】三件，
  不另立一套。规范见 `docs/技能包规范_20260920.md`，实现 `skill_pack.py`。
  - **权限模型三层**：① 永禁（os.system/eval/winreg/rmtree… 写任何权限都不放行）
    ② 分级关键词需声明（subprocess→process；urllib/requests→network；os.remove/shutil.move→files.write）
    ③ 安装时列出权限卡片由你确认，**未确认的包不加载**；`builtin: true` 的官方包视为已授权
  - **三种安装来源**：本地目录 / zip / GitHub 仓库；校验含清单字段、语法、永禁与分级扫描、
    zip-slip（`../`、绝对路径、盘符）、扩展名白名单、文件数≤200、体积≤5MB；落盘登记来源+哈希
  - **可管理**：启用/禁用热生效、来源与版本登记（`plugins/_registry.json`）、
    **卸载改为移进停放区 `plugins/_uninstalled/`**（保留最近 5 个可找回，不再 `rmtree` 直接删）
  - **老插件兼容**：`manifest_version` 缺省（v1）只拦永禁、不做分级，避免误伤；
    本轮实际踩到 `vision` 插件（用 urllib 但没声明）被新策略拒载 → 已修，并给 vision 补上 v2 清单 + network 声明
  - **顺手修一个旧缺陷**：被安全策略拒载的插件以前仍会挂进 `plugins` 列表（module=None，看着像加载了），
    现在直接不挂，并把拒绝原因记入 `PluginManager.rejected` 供界面展示
  - **首个真实官方技能包**：`plugins/office_report/`（工具 `excel_report`）—— 声明 office.com + files.write，
    真机跑通：模型调用 → WPS `KET.Application` 生成 xlsx（9.6 KB）
  - **首批官方技能（v6.67，Batch 3-2）**：
    - `pdf_tools`（`pdf_tool`）：合并 / 按页拆分 / 提取文字 / 看信息（pypdf，纯本地不联网）
    - `image_batch`（`image_batch`）：等比缩放 / 转 jpg·png·webp / 按序号重命名 / 看尺寸（Pillow，**只复制不改原图**）
    - `file_organize`（`organize_files`）：按类型或修改年月归类 / 找重复文件（MD5，**只报告不删**）/ 批量重命名。
      **先预览后执行**：`run`、`rename` 必须带 `confirm=true` 才真动文件
    - **Word / PPT 写入**（补进内置 `office_doc`）：`write_doc`（标题+段落+表格，python-docx）、
      `write_slides`（封面+要点页+讲稿备注，python-pptx）—— **不依赖本机装 Word/PPT**
  - **本轮端到端抓出并修掉 3 个真 bug**：
    ① PPT 被写成 `.xlsx`（`_office_resolve` 的扩展名白名单没含 `.pptx`，被“修正”成了默认扩展名）
    ② PDF 合并失败（AI 给的 Windows 反斜杠路径放 JSON 里会因 `\U` `\t` 报错，
    一报错就把整串当成一个路径）→ `_load_paths` 改为“先 JSON、失败再按分隔符切”
    ③ PDF 拆分忽略传入的输出名 → 改为优先用 `name` 作前缀
    ④ **PDF 提取文字会把乱码当正文交出去**：字体没带 ToUnicode 表的 PDF（扫描件、某些生成器）
    能“提取出”一堆无意义字符 → 新增 `_looks_garbled()` 判定，命中时明确告知“像乱码、建议 OCR”
  - 测试：`tests/test_official_skills.py` **19 项**；全量 **404 passed**

- **MCP 接入：修了旧 bug + 管理界面 + 权限确认（v6.68，Batch 3-3）**
  - **修了一个一直存在的老 bug**：解析工具时写的是 `t.input_schema`，而本 SDK 字段名是
    `inputSchema` → 抛 `AttributeError`、连接直接挂、**工具数永远是 0**。
    也就是说 MCP 桥接以前**从来没能真正用起来**——只有拿真实 MCP server 去接才会暴露
  - **真机验证**（第三方 server `uvx mcp-server-time`）：连上 → 2 个工具 → 合并成 `mcp_time_*`
    → 真调 `get_current_time` 拿到实际时间（2026-09-20T13:02:19+08:00）
  - **权限策略**：① server 自己的 `readOnlyHint` / `destructiveHint` 注解优先
    ② 否则看工具名动词（read/list/get… 只读；write/delete/exec… 写类） ③ 看不出→**保守要求确认**
  - **写类工具调用前问你**（默认开，可在设置→MCP 关）：跨线程弹确认框（复用现有 `_request_confirm`），
    拒绝则中止并回一句“使用者拒绝了这次 MCP 工具调用”
  - **管理 API**：加/删/启停（热断连）/重启/查看工具与权限/配置持久化（原子写，不整文件覆盖）
  - **推荐清单**（一键添加，全部默认关）：时间 / 文件系统 / 网页抓取 / SQLite / Git
  - **设置新增「MCP」页**（设置共 **10 页**）：列表（名称·传输·状态·工具数）+ 推荐清单一键添加 +
    加 stdio / 加 HTTP + 启停 + 测试连接 + 工具与权限 + 删除 + “写类工具调用前先问我”
  - 断开时的 `CancelledError` 噪声也吞掉了（正常路径不当错误）
  - 测试：`tests/test_mcp.py` **20 项**（含本地假 MCP server 走真 stdio 协议）；全量 **424 passed**

- **治理三件：审计日志 / 出网白名单 / 额度限制（v6.69，Batch 3 收尾）**
  使用者原话：把“敢用”变成“敢放心用”。三者共享**一份事实来源**（当日审计日志）：
  - **审计日志**（`governance.py`）：技能/MCP 的安装、授权、启用、调用、拒绝、连接失败都落一条
    JSONL（`logs/audit_YYYY-MM-DD.jsonl`，默认留 30 天）；字段含时间/对象/动作/参数摘要/结果摘要/是否放行/耗时
  - **额度限制**：**直接数当日日志**里的调用次数（不另存计数器，避免两套数据对不上）；
    默认单对象 20 次/分、300 次/天（可在 `tool_limits` 里改），超限直接拒并记一条 `deny`；被拒的调用不占额度
  - **出网白名单**：新增 `pet_net.py` 作为技能包唯一出网入口（`http_get` / `http_post_json` / `get_json`…），
    入口先查 `net_allowlist`（域名模式、支持 `*.example.com`）；**空白名单 = 技能一律不许出网**；
    响应限 4 MB；放行与拒绝都写审计。配套约束：v2 且非 builtin 的技能包**直接用 urllib/requests 会被安装时拒绕**
    （同进程拦不住 import，所以改成强制走统一入口，让白名单真能起作用）
  - **界面**：设置 →「技能」「MCP」两页均新增「🧾 审计日志」按钮（看最近 40 条 + 今日拒了几条）；
    技能页底部常驻显示“今日审计 N 条、拒 M 条”
  - **审计不变量测试**（`tests/test_audit_invariants.py`，8 条）：把这次统一审计的结论固化成断言 —— 
    清单合规、无拒载、新包不直连网络库、内置条目集合、工具/页数与文档一致、**开设置不写配置**、
    治理默认值安全、pet_net 入口齐备。以后任何改动破坏它都会直接红
  - 测试：`tests/test_governance.py` **16 项** + MCP 接线 3 项 + 不变量 8 项；全量 **444 passed**
  - **模型侧工具**：新增 `skill_pack`（进阶工具集，30→31 个工具）
  - **设置新增「技能」页（v6.67）**：列表（名称/版本/来源/有效状态，悬浮看权限）+ 刷新 / 从目录安装 /
    从 zip 安装 / 启用禁用 / 权限（看卡片并可当场确认）/ 卸载；被安全策略拦下的包以 `⚠` 行展示原因
  - 测试：`tests/test_skill_pack.py` **48 项** + 技能页 2 项；全量 **385 passed**

- **语音输入（v6.66，第三件事）**：新增 `asr.py` + `asr_helper.ps1`（WinRT SpeechRecognizer，**离线、零依赖**）。
  - 聊天输入框左侧新增 **🎤 按钮**（右键 → 💬 互动 → 「🎤 语音输入」同功能）：点一下开始听、
    再点一下取消；识别完**只填进输入框、绝不自动发送**（保留你确认这一步）
  - **能力检测**：先查本机能不能识别（没有语言包/没有脚本都直接说原因），不静默失败
  - 实测发现（如实告知）：本机 WinRT 识别被 Windows 的**语音隐私策略**拦下
    （`The speech privacy policy was not accepted`），对应开关是
    **设置 → 隐私和安全性 → 语音 → 在线语音识别**；这是隐私设置，**代码不代改**，
    只把指引写进提示（点 🎤 会直接告诉你去开哪个）。若不想开该开关，可用系统自带的 Win+H。
  - 错误路径全覆盖：超时（会先 kill 子进程再提示）、麦克风权限被拒、无内容、取消
- **办公文档技能（v6.66，第四件事）**：新增 `office_skill.py` + `office_doc` 工具（已入**默认可用**工具集）。
  - 走**官方 COM 接口**，优先 WPS、退回 MS Office：`info` 探测接口 / `write_sheet` 写表格 /
    `read_sheet` 读回 / `make_report` 生成带样式报表（标题合并居中、表头加粗填色、全表边框、列宽自适应）/
    `export_pdf` 导出 PDF
  - **路径安全**：AI 给的路径一律收敛到程序目录 `输出/` 下（只取文件名，防穿越），与 `write_file` 同一策略
  - 实测（本机）：探测到 WPS KET/KWPS/KWPP v14.0；真实生成 `桌宠工具链路自测报表.xlsx`（10KB，
    带样式）+ 导出 PDF（37.4KB），读回校验一致；验证产物已交付给使用者
  - 意义：原先 29 个工具全是通用/系统类，`write_file` 只能出纯文本 —— **日常办公任务做不了**；现在补上了

原先「模型与 API」一页 **26 行**（模型 / 计费 / 语音 三件事混在一起），
  拆出「**模型**」「**语音**」「**用量与计费**」三页 —— 现在单页最多 13 行，找东西靠分类就能定位。
  控件变量名全部保留（只改挂载位置），并补了回归测试：关键控件各归其页、单页行数 ≤ 14。
  （v6.67 又加了「**技能**」页、v6.68 加「**MCP**」页 → 共 **十页**：通用 / 对话 / 外观 / 模型 / 语音 /
  用量与计费 / 记忆与数据 / 技能 / MCP / 系统）
- **右侧信息栏：一栏多用 + 页签可自定义增删（v6.65）**：新模块 `side_panel.py`。
  - 默认三页签：**📋 任务**（FCFS 队列，拖拽排序/双击取消/`/stop` 急停）、**📊 状态**（今日用量·余额·
    当前模型与声线·峰谷时段·好感）、**✔ 待办**（清单概览）
  - **可自定义**：右上「＋」加页签（状态/花费/待办/**便签**/系统），页签上右键可**重命名 / 删除**、
    可拖拽排序；便签可加多个、自动保存
  - **记忆**：页签列表（含便签文本）与折叠状态存进 `config.json`（输入时 800ms 防抖，不会把盘写爆）
  - **省地方**：折叠后只留一个竖条把手；**没有任务时不再白占 1/3 宽度**（旧版只有任务且一直占位）
  - 保留旧属性名（`chat_task_sidebar` / `task_list` / `task_collapse_btn`），外部接线不受影响
  - 例外说明：**任务页签不可删** —— 队列的拖拽排序与双击取消必须有界面；其余页签均可删
- **修 3 个“天黑就红”的测试（v6.65）**：`test_care_schedule` 的两项与 `test_care_bubble` 一项在 23:00 后
  运行会被“深夜静默”提前拦下而失败（测试不锁时间的副作用）。已改为在用例内屏蔽深夜判断，
  与运行时刻无关；语音测试也改成显式 `night_quiet=False`。
- **接入本地自建 TTS 服务（v6.64）—— “自己训练/克隆的声音”可直接接**：
  使用者问：“自己录音或自训练模型难道还得另起一套工程？”。结论：**不用** —— 这类项目
  （GPT-SoVITS / CosyVoice / ChatTTS / IndexTTS…）都自带 HTTP 接口，所以我们只需要一个适配器。
  - 声线支持 `local:<地址>`（如 `local:http://127.0.0.1:9880`），约定 `POST {地址}/tts`
    返回音频（兼容 GPT-SoVITS 的 api_v2 风格；请求体带 `text` / `ref_audio_path` / `prompt_text`）。
  - **逐级降级**：本地服务 → 在线 edge → 离线系统声线；服务没开时自动降级并**把原因留下**。
  - 设置页新增「本地服务地址 / 参考音频 / 参考文本」+「🔌 测试连接」+「🎙 用本地服务试听」。
  - 实测：起一个**真实 HTTP 服务**跑通全链路（请求体校验、WAV→winsound、MP3→MCI、
    服务不可达时降级）。
  - 顺带说明：现在主流支持**零样本克隆**（3～10 秒参考音频）—— 多数情况**不需要训练**，
    “训练”只在想要更稳定/更像时才做（微调几十分钟）。
- **修正测试卫生问题**：`tests/test_stream_cancel.py` 之前直接赋值 `urllib.request.urlopen`
  且**从不恢复**，会污染后续用例（本次排查本地 TTS 用例时被这个坑缠住：后续用例的 urlopen 拿到的是
  伪造的 SSE 流）。改用 `monkeypatch.setattr` 自动恢复。
- **朗读延迟大幅下降 + 消除控制台闪窗 + 自定义声线（v6.64，同一条反馈的三点）**：
  - **句子级流水线降延时**：把文本按句切分，**第一句合成完就开声**，而**在它播放期间并行合成下一句**。
    实测：三句话的回复**首句出声 2.58 秒**（改造前要等整段合成完，5～7 秒）；
    单测锁住这个时序（若改成“整段先合成”或“等播完再合成下一句”，测试会红）。
  - **不再闪控制台窗口**：离线合成的 PowerShell 子进程加上 `CREATE_NO_WINDOW`（声线列举同）——
    之前会先弹一个黑框再出声，很突兀。
  - **自定义声线输入**（设置 → 朗读声线 → 自定义声线）：
    · `offline:<声线名>` —— 用户自己在 Windows 里装的语音包也能直接用（自动去掉 `Microsoft ` 前缀）
    · `edge:<声线名>` —— 任意语言都行（如 `edge:ja-JP-NanamiNeural` / `edge:en-US-AriaNeural`）
    · 「📃 列出系统声线」真调 `tts_helper.ps1 -ListVoices`（WinRT + SAPI，自动去重）；
      「🌐 列出在线声线」真调 edge-tts 声线表（实测本机 322 个、其中中文 8 个）
  - 降级留痕：一旦“在线失败转离线”，把原因写进 `last_error`（不让用户莫名其妙发现音质变差）。
- **在线神经声线成为默认朗读引擎（v6.64）**：使用者反馈“离线声线听起来过于机械，甚至不如 edge-tts，毛骨悚然”。
  - **默认走 edge-tts**（微软神经声线，24kHz MP3）：内置晓晓（与助手同款）/晓伊/云希/云扬/云夏五个中文声线。
  - **播放改用系统 MCI**（ctypes 调 winmm）—— 因为 `edge_tts` 输出 MP3 而 `winsound` 只能播 WAV。
    实测：`open/play/status/stop/close` 全部成功，可随时打断；**仍然零 pip 依赖**。
  - **失败/断网自动降级**到离线系统声线（WinRT → SAPI）并在内部记下“已降级”，不假装没问题。
  - **`edge_tts` 惰性导入**：没装或没网时模块照样能跑（直接走离线），不会因缺包而报错。
  - 声线配置改为 `引擎:声线`（如 `edge:zh-CN-XiaoxiaoNeural` / `offline:Huihui`），设置页一个下拉包全。
  - **打包注意**：出包命令需加 `--collect-all edge_tts`（已实测：迷你包内 edge_tts 可用，
    列出 322 个声线并成功合出 MP3）；漏掉也能跑，只是朗读自动降级为离线声线。
- **语音朗读（v6.63，P0）**：AI 回复可自动念出来。新增 `voice_io.py` + `tts_helper.ps1`：
  - **离线兜底**：合成走系统内置 WinRT 声线（Huihui / Yaoyao / Kangkang），失败自动回退 SAPI。
  - **默认关**：关着的时候**连脚本都不启动**（零子进程、零音频）；右键可一键开关。
  - **夜间静音**：默认 23:00–08:00 不出声（按**本机本地时间**判断 —— 计价看官方口径，静音看人的生活作息）。
  - **文本清洗**：朗读前剥掉 `[emotion:xxx]` 标签、Markdown 记号、代码块、URL、表情，并截断到 300 字（不念代码）。
  - **顺序队列**：同一时刻只播一句，后续排队；“朗读上一条”与“试听”走 `force`（用户主动要求就该出声）。
  - 实测：WinRT 与 SAPI 两条路都产出有效 wav（16k/22.05k 单声道），winsound 异步播放与打断均可用。
- **搜共同经历（v6.63）**：聊天端新增 `/回忆 关键词`，回忆相册加搜索框。
  新模块 `memory_search.py`：事实类走既有 BM25 管线（**刻意不过闸门** —— 闸门是为“自动注入”
  设计的宁缺毋滥，用户主动搜就该尽量给结果）+ **单字查询子串兜底**（如搜「猫」），
  事件类按 2-gram 关键词打分；两类**分组展示不混排**；
  **纯本地、默认不调 LLM 重排 → 零 API 成本、即时返回**（需要更聪明的排序时可传入重排回调）。

### 测试

- 新增 `tests/test_memory_search.py`（11 项）与 `tests/test_voice.py`（11 项），全套 **266 passed**。

## [2.8.0] - 2026-09-19

### 修复

- **接第三方模型时的计费不再“默认按 DeepSeek 算”（v6.62）**：使用者提出“用户偏要用别家 API 接入怎么办”：
  - **计价方式跟档案走**：档案新增 `pricing_mode`（`auto` / `deepseek_peak` / `flat`）。`auto` 按
    接口地址判定 —— 官方域名走峰谷，中转/第三方走**固定价**（治“第三方模型被动跟着 DeepSeek 峰谷翻倍”）。
    模型管理里可手选。实测：第三方档案 100k in/10k out 在官方高峰与空闲均为 **1.3000 元**（不再×2）；
    官方档案仍 0.1400 → 0.2800 ✓
  - **未知价不再编数字**：模型没配价格时不再拿 DeepSeek 兜底价估出一个“看着像真的”的费用，
    而是记 **0 + `price_unknown` 标记 + 单独计数**；悬浮窗新增一行「⚠ N 次调用未配价格，未计入费用」
    （单独成行，避免被按像素收口截断），首次出现时状态条也提醒一次，引导去模型管理填价。
  - **新增「💰 核对官方价格」**（模型管理）：官方无机器可读的定价接口，但定价页是服务端渲染的规整表格，
    可直接抓取解析（实测能稳定读出 6 组数字）。按钮会拉取官方价并与本地档案比对，
    **只展示差异、不自动改价**（避免官方改版把本地价写坏）；用户确认后才写入，
    且**只影响官方地址的档案**（第三方/中转档案不动）。实测：解析结果与本地档案 **0 差异**。

- **峰谷计价不再依赖用户时区与本机时钟（v6.62，新增 `server_clock.py`）**：原先判定用
  `datetime.datetime.now()`（本机当地时间），有两个坑：
  ① **外国用户**（系统时区不是 Asia/Shanghai）拿到的是当地时间 → 判错；
  ② **改过系统时间 / 时钟漂移** → 不只小时错，连日期都可能错，还会把节假日表选错年份。
  对策：
  - **换算**：一律走 UTC + 固定 **+08:00** → 北京时间（中国无夏令时，固定偏移完全等价，
    且不依赖 Windows 缺失的 tzdata —— `ZoneInfo('Asia/Shanghai')` 在 Windows 会报错）。
  - **校正**：用官方 API 响应头 `Date`（RFC 1123、GMT）反推本机时钟偏差 —— **零成本**，
    每次调用 API 顺带校正一次，不额外发请求、不消耗额度；偏差 > 5 年视为异常（代理伪造）忽略。
  - **可观测**：统计悬浮窗新增「时段: 高峰/空闲（北京 MM-DD HH:MM）」一行；设置页峰谷区显示
    “北京时间规则 + 换算到你本机的时段 + 本机时钟偏差（> 1 分钟才提示）”。
  - 实测：真请求后自动记到服务器时间（偏差 -0.4 秒）；模拟 UTC+0 用户在 02:00 UTC → 正确判为
    北京 10:00 高峰；模拟本机时钟快一年 → 校正回真实年份后按 2026 年周日判为空闲。

- **余额查询报 HTTP 404（v6.62，使用者反馈）**：档案里存的是**完整接口地址**
  （`https://api.deepseek.com/chat/completions`），而 `query_balance` 直接拿它拼 `/user/balance`
  → 实际请求 `…/chat/completions/user/balance` → 404。上一轮验证时我传的是**裸基础地址**，
  所以没暴露（教训：验证要贴近真实调用路径）。**修复**：新增 `ApiStats.normalize_base()`，
  先归一成基础地址（能认出 `/chat/completions`、`/v1`、`/beta` 后缀，也保留中转网关的自定义前缀），
  `query_balance` 统一通过它拼地址。实测：用档案里的完整 endpoint 查余额 → `ok=True`。
- **API 统计悬浮窗拖不动、像卡死（v6.62，使用者反馈）**：两个叠加原因 ——
  ① v6.61 新加的余额行让面板内容变宽变长（布局最小宽 361px / 更胖的来源行能到 620px），
  而窗口是写死的 `320×142` → 内容装不下、布局持续挤压重排；
  ② 拖拽只挂在 `panel` 上，抓到按钮或边缘区域完全没反应。
  **修复**：① 高度按内容算，宽度取“内容需要与基准宽的较大者（上限 460）”，
  并新增一道**按像素收口**（超宽的行自动省略号，完整信息进 tooltip），任何一行都不再能顶宽窗口；
  ② 拖拽改为事件过滤器，装在窗口及其所有子控件上 —— **任意位置按住即可拖**，
  拖动期间暂停 1 秒刷新（避免刷新与移动互相打架），松手把位置写进 `stats_win_pos` 下次恢复；
  ③ 查询按钮限定 150px 宽并居中，四周留出可拖区；窗口加 tooltip 提示。
- **价格表与官方不符（v6.62 核对）**：原 flash 价 `1.5/0.05/4.5` 与官方都不对（pro 的 `4.5/0.15/13.5`
  正好是官方**空闲时段价**）。按官方定价页（api-docs.deepseek.com/zh-cn/quick_start/pricing，
  2026-09-19 核对）校正为 flash `1/0.02/4`、pro 不变，并新增**峰谷分档**（见下）。

### 新增

- **价格分峰谷计价（v6.62）**：官方高峰时段（北京时间周一至周五 9:00–12:00、14:00–18:00）价格为空闲的两倍。
  `ApiStats.is_peak_now()` 按时段自动切价；档案价格新增 `input_peak/cache_peak/output_peak`
  三项（缺失时按“高峰 = 空闲 × 2”推算）。
- **峰谷计价认法定节假日（v6.62，新增 `cn_holidays.py`）**：官方口径是“高峰 = 周一至周五，
  **不含法定节假日**；其余（周末 + 节假日全天）为空闲”，而原先本地判不了节假日，只能保守按高峰算（多花钱的估算）。
  现补齐：
  - **判定规则**：只看“是不是周末 / 是不是法定节假日”，**不看调休补班** → 调休补班的周末仍按空闲
    （使用者从官方群确认，与官方定价页脚注一致），因此只需要“放假日”日期表，不需要补班表。
  - **数据源与成本**：内置 2025–2026 放假日表（国务院安排，holiday-cn 校对）；年度更新拉
    `NateScarlet/holiday-cn` 的**公共静态 JSON**（无 key、无调用额度，单年 < 2KB）。
    **不使用搜索 API** —— Tavily 每次搜索都吃额度（1000 次/月免费额度），查这个纯浪费。
  - 跨年时（当年不在表里）启动后静默拉一次；设置 → 模型与 API → 「峰谷计价 → 📅 更新节假日表」可手动更新。
  - 实测 11 个用例符合官方口径：国庆/中秋/春节全天空闲、普通工作日 9–12/14–18 为高峰、
    午休与晚上为空闲、六个调休补班的周末均为空闲；表里没有的年份（如 2027）保守按高峰。
  - 其他核实：官方峰谷定价自 2026-08-17 生效，且 2026-08-23 又调过一次（周六周日全天不再区分峰谷，
    统一低谷）——与代码实现一致。
- **strict 严格工具参数（v6.62，官方 Beta）**：新增设置项「严格工具」。开启后请求走
  `/beta/chat/completions`，并把**能安全严格**的工具转成 strict（规范化 schema：递归剔除
  `minLength/maxLength/minItems/maxItems/pattern/format` 等白名单外关键字、对象补
  `additionalProperties: false`）。选工具原则（实测得出）：
  - 只收「required 已覆盖全部属性」的工具（全部必填，共 20 个）；有可选参数的
    （`edit_own_code`/`read_file`/`memorize` 等）保持原样 —— strict 要求全字段必填，强行改会改语义。
  - **`offer_choices` 必须排除**：它的 `items` 是空 schema `{}`，官方直接 400
    （`one of type, anyOf, $ref field is required`）。实测坐实后才加了 `_has_anonymous_subschema()` 拦截。
  - 实测：桌宠实际报文（30 个工具、20 个 strict）发 `/beta` → HTTP 200 且正常返回 tool_calls。
- **历史思考内容回传（v6.62）**：官方称带 `tools` 的请求应回传历史 `reasoning_content`。新增
  `_remember_reasoning()` / `_with_reasoning_history()`：只记上一轮、截断 4000 字符、按内容匹配补回，
  **不改动历史原表**。设置项「思考回传」（默认开）。实测：补回后发请求 → HTTP 200。
- **图片走 Files API 复用（v6.62）**：新模块 `files_api.py` —— 官方 `POST /files`（multipart，
  `purpose=user_data`）+ 按内容哈希的本地缓存 `files_cache.json`，同一张图只上传一次，之后消息里用
  `{'type':'file','file_id':…}` 引用。设置项「图片复用」（默认关）。实测：上传 → 用 file_id 提问，
  模型正确读出图里文字；同一张图第二次提问**复用同一 file_id**（未重传）；上传失败自动回退 base64 直发。
- **模型管理页**：思考强度、视觉能力两项控件；价格区新增“空闲/高峰”说明。
- **设置页新增三项 API 开关**：思考回传 / 严格工具 / 图片复用（均带 tooltip 说明代价）。

### 核实（非代码变更）

- **v4-pro 仍在提供**：`probe_model` 实测 `deepseek-v4-pro` 的响应 `model` 字段仍是 `deepseek-v4-pro`
  （官方新闻页与 Change Log 互相矛盾那件事 —— 以实测为准）；旧名 `deepseek-v4-flash`、
  `deepseek-v4-flash-vision-exp` 都归一为 `deepseek-flash`（官方定价页脚注亦如此说明）。

- **思考开关/强度真正进请求体（v6.62）**：设置里的「思考模式」此前只驱动 UI 勾选框，**从未进过请求体**，
  而官方默认就是 `thinking=enabled` + `effort=high` —— 所谓「关掉思考」根本没关，token 照花、速度照慢。
  实测对照（流式通道，问「1+1」）：关思考 → 只出 `content`，**1 个 token** 答完；不关 → 先吐 33~40 段
  `reasoning_content` 再答，35~40 token。
  - 请求体现在统一由 `PetWidget._thinking_fields()` 产出：关思考（或强度 `none`）→ 只发
    `thinking:{'type':'disabled'}`；开思考 → 同时带 `reasoning_effort`（none 与 enabled 自相矛盾，归一成 disabled）。
  - 模型档案新增 `effort`（`none/low/high/max`，旧别名 `minimal→low`、`medium/xhigh→high` 自动归一），
    设置 → 模型与 API 加「思考强度」下拉；「模型管理」同步支持。
  - 空回复重试的第二次请求显式关思考（该直接说话的场合不再白烧思考 token）。
  - 采样温度旁如实标注「官方在思考模式/多模态下不支持采样温度，此项仅关思考后生效」（此前调了等于没调）。
- **图片直接交给模型看（v6.62，视觉）**：新增 `vision_helper.py`，把本地图片按官方多模态格式
  （`content` 数组 + `image_url` + base64 data URL）随消息直送模型，替代原先「一律先本地 OCR 成文字」
  —— 图表趋势、界面布局、示意图这类非文字信息不再丢失。
  - 体积可控：长边缩到 1568px、单图 ≤ 2MB（先无损 PNG，超限转 JPEG 并逐级降长边），一条消息最多 4 张图。
  - 打通三条入口：拖入/粘贴图片、附件列表里的图片、全局快捷键全屏截图（Ctrl+Alt+D）。
  - 失败自动回退本地 OCR（编码失败/非官方地址不静默丢图），并在状态下告知用户。
  - 按档案 `vision` 字段判定能力（`deepseek-flash` 支持、`deepseek-v4-pro` 不支持；档案未声明时按官方
    能力表兜底），设置 → 模型与 API 新增「图片直送」开关，「模型管理」新增「视觉能力」勾选。
  - 实测：造一张 4 根柱子的柱状图直送模型，模型答对「图里有 4 根柱子，最高的是 200 那根」（OCR 做不到）。
- **JSON 结构化输出（v6.62）**：记忆抽取（`_extract_chat`）与记忆重排（`_rerank_memories`）改用官方
  `response_format={'type':'json_object'}`，不再靠「让模型吐 JSON + 正则抠」（抠失败即静默丢记忆）。
  重排输出改为 `{"ids": [...]}` 并新增 `_parse_rerank_ids()`：JSON → 花括号内 JSON → 抠数字三级容错，
  旧格式回退仍可用。

### 测试

- 新增 `tests/test_api_options.py`（17 项）：思考三态与归一、**真跑一遍 `_ai_worker` 抓实际请求体**
  断言 `thinking`/`reasoning_effort`/`temperature`、空回复重试护栏、图片编码往返与 4000×3000 大图压缩
  （长边 ≤ 1568、字节 ≤ 2MB）、`build_vision_content` 形态、视觉失败回退 OCR、三条调用点与任务队列
  带图护栏、抽取/重排 `response_format` 断言、`_parse_rerank_ids` 七种输入、档案 `effort`/`vision` 往返
  与官方能力表兜底。全套 **189 passed**（原 172）。

## [2.7.3] - 2026-09-19

### 修复

- **扒边状态下主动关心会「自己弹出来、而且弹出来就不缩回」（v6.61）**：使用者反馈桌宠缩回（扒边）后
  过一会儿又自己弹出来，且长时间杵在全尺寸状态。根因是 v6.60 批 3 加的「扒边探头」把弹出动作写在了
  `_check_active_chat()` 的**开头**，而节流判断在其**之后**：
  - `active_chat_timer` 每 30 秒跑一轮 → 无条件 `_popup_from_dock()` + `_dock_rehide = True`；
  - 随后 `now < self._active_chat_next`（距下次关心还有 8–20 分钟）或 `_care_allowed()` 不通过
    （冷却 20 分钟 / 当日上限 8 次 / 深夜静默）→ 直接 `return`，一句话都没说；
  - 而收回动作只写在 `_hide_bubble()` 里（靠「气泡消失」触发），没有气泡就永远不会收回
    → 桌宠停在全尺寸弹出态；下一轮 30 秒检查时 `_edge_popped` 已是 True，不再探头也不再收回。
  最小复现（stub 直接调 `PetWidget._check_active_chat()`，`_active_chat_next` 设在 10 分钟后）：
  弹出=`True` / 说话=`False` / `_dock_rehide`=`True`，坐实顺序问题。
  **修复**：① 把探头挪到「确认这一轮真的要说话」之后（节流不通过的轮次不再触碰窗口几何）；
  ② 探头即挂 60 秒看门狗（`PROBE_WATCHDOG_MS`），到点若仍没说出话（AI 判定不说话 / 线程异常 /
  文本为空）→ 强制收回；③ 收回路径收敛为 `_rehide_after_probe()`，由 `_hide_bubble()` 与看门狗共用，
  并在探头发起/收回各记一条 `log.info`（此前只有 debug 级，用户侧看不到）。

### 修复

- **API 费用提示与对话气泡重叠、且一闪就没了（v6.61）**：使用者反馈「对话后冒的气泡会和 API 扣费气泡
  相互重叠，而且扣费气泡基本上一秒内就消失、根本看不清」。原因：v6.30 的费用气泡是窗口顶部 y=8 的
  `CostBubble` 浮动动画，而说话/关心气泡也贴在窗口顶部 y≈6，两者同区必然叠；且 `show_bubble()`
  默认 1400 ms 全程上浮+渐隐（越到后面越淡）。
  **修复**：① 费用提示改走**底部状态条**通道（`_notify`）—— 位置与对话气泡不冲突、停留
  `COST_NOTIFY_MS = 6000` 毫秒、内容从单个金额扩为「本次 −¥0.0032 · 今日 ¥0.42 / 32 次」；
  ② `CostBubble.show_bubble()` 新增 `hold_ms`：先原地静止满不透明，再上浮渐隐（`QSequentialAnimationGroup`
  + `QPauseAnimation`），保留在好感/喂食等浮动提示上；③ `_show_pet_bubble()`（喂食/好感）遇到
  说话气泡正在显示时自动下移到气泡下方，不再叠在一起。

### 新增

- **「扒边时弹出来说」开关（v6.61，默认关闭）**：把「弹不弹人」交给用户决定。
  - **关闭（默认）**：扒边说关心时**只冒气泡，人保持扒边**（`_place_bubble` 已按窗口实际宽度夹过，
    长句不会被硬裁）；
  - **开启**：整人弹出说话，说完自动缩回扒边。
  - 入口：右键菜单 → **🪟 扒边时弹出来说（已开启/已关闭）**；设置窗口 → 通用 → **扒边时弹出来说**。
    落盘字段 `dock_probe`，走既有 `_save_cfg_value` 通道热加载；AI 工具改配置白名单
    （`CONFIG_WHITELIST`）与状态字典同步收录。
- 顺手：点关心气泡展开到聊天时，若正处于扒边状态则先弹出（否则聊天面板在 260px 窄窗里被截断）。
- **余额查询（v6.61）**：使用者反馈「光检测扣费用处不大，能拿到余额数据更好」。官方
  `GET /user/balance` 已实测可用（HTTP 200，返回 `total_balance` / `granted_balance` /
  `topped_up_balance` / `is_available`）。
  - **数据层**：`api_stats.query_balance(key, base_url)`（纯函数、无 UI）；缓存写进 `api_stats.json`
    的 `balance` 字段（含查询时间），不写 config、不动 key；**失败绝不编数字**，只回 `error`。
  - **触发**：① 右键「📈 API 用量」→「💰 查询余额」（手动，结果落底部状态条）；
    ② 每次对话后若缓存超过 `balance_auto_minutes`（默认 10 分钟）→ 后台静默刷新（不弹提示）；
    ③ API 统计悬浮窗新增余额常驻行（含「多久前更新」）+「💰 查询余额」按钮。
  - **费用提示升级**：状态条文案变成「本次 −¥0.0032 · 今日 ¥0.42 / 32 次 · 余额 ¥70.68」。
  - **低余额提醒**：低于 `balance_low_threshold`（默认 ¥5，设置→模型与 API 可手改，0 = 关闭）时
    提醒一次，每天最多一次。
  - 非官方地址（中转）会明确提示「不支持余额接口」，不会给出错误数字。

### 测试

- 新增 `tests/test_dock_rehide.py`（**9 项**）：节流未到点/冷却中不得探头、开关关闭只冒气泡、
  开关开启探头并挂看门狗、气泡说完自动缩回、看门狗超时强制收回、开关落盘、扒边窄窗气泡宽度、
  源码护栏（探头必须排在节流判定之后）。改写 `tests/test_care_bubble.py`
  的 `test_dock_probe_toggle_controls_popup`（原「必须先探头」锁的是旧行为，按新设计改为「开关决定是否弹出」）。
- 新增 `tests/test_cost_notify.py`（**5 项**）：费用提示落在状态条且停留 ≥6 秒、不再创建顶部浮动气泡、
  说话气泡可见时浮动提示自动下移、`hold_ms` 先静止再渐隐（且不传时与旧行为一致）、源码护栏。
- 新增 `tests/test_balance.py`（**17 项**）：官方响应解析、/v1 地址规整、HTTP 错误/缺字段/无 key 的
  失败路径（且不编数字）、缓存落盘与失败不覆盖、过期判定、手动提示、自动静默、低余额每日一次与
  阈值 0 关闭、费用提示带余额、阈值落盘与夹取、源码护栏。
- 全量：**172 项通过**（原 141 项 + 新增 31 项，其中 1 项为改写）。
- 端到端冒烟（offscreen 真对象）：开关关 → 窗口保持 260×340、气泡 `care` 模式；开关开 → 窗口
  440×560 弹出、看门狗计时中、气泡结束后缩回 `left`；看门狗超时 → 强制收回且标记清零。

## [2.7.2] - 2026-09-17

### 新增

- **关心的节流与场景触发（v6.60，批 4/4）**：此前只有「到点」一个触发条件，没有任何上限，
  且无 AI 时兜底只有 4 句固定台词（「该喝水啦～」反复出现）。本批补上触发与节流两头：
  - **场景触发**（接 v6.59 的前台程序感知，仅开启时生效）：前台程序从「专注」（写代码/办公/会议/游戏/视频）
    切到「不表态」（浏览器/终端/其他）→ 视为**休息间隙**，在 30–300 秒窗口内可**提前**关心一次；
    同一间隙只用一次，且仍受下面的三重节流约束。
  - **三重节流**（`_care_allowed()`）：① 深夜静默 23:00–8:00（此前只是提示词里的口头规则，现改为硬约束）
    ② 冷却 `CARE_COOLDOWN_MIN = 20` 分钟 ③ 每日上限 `CARE_DAILY_MAX = 8` 次；未通过则顺延 5 分钟再看，
    并记一条 debug 日志。计数为内存态（重启重置，不写用户 config）。
  - **措辞多样化**：`care_engine.fallback_lines(lang, kind)` / `pick_fallback(...)` —— 18 句按意图分组
    （休息 / 闲聊 / 关心，中英各一套），调用侧避免与上一条重复；链式判断也把「最近我主动说过什么」
    一并交给模型（`_ai_wakeup_judge` 注入最近两条），要求换个角度、不要重复。
  - 实测（可控时钟压缩一整天 09:00–21:00，每步 30 秒）：关心 **8 次后触顶上限**、之后不再触发；
    其中 10:31 那次为切到浏览器后的场景触发。专项验证：把「下次到点」推到 1 小时后，
    从 PyCharm 切到 Chrome **30 秒后**即触发一次关心。

- 新增 `tests/test_care_schedule.py`（**9 项**）：深夜静默边界（23/8 点）、冷却放行与拦截、
  每日上限与跨天重置、计数递增、场景触发需开启前台感知、窗口 30–300 秒与单次使用、
  仅「专注→不表态」才开窗、兜底库≥15 句且不连续重复、节流接线源码护栏。

- **关心通道独立成层（v6.60，批 3/4）**：此前主动关心 / 回访 / 提醒与系统提示**共用同一个气泡外观**，
  且提醒类还「气泡 + 聊天列表」各写一遍（重复）；关心内容因为防污染不写任何记录，
  事后无法回看；扒边未弹出时 `_check_active_chat` **直接 return**（这一次关心就没了）——
  四件事共同造成「开了主动关心会混在一起、效果不明显」。本批逐一解决：
  - **独立外观**：新增 `say_care(text, kind)` —— 气泡左侧加 **accent 色条**（`pet_bubble.apply_say_bubble_theme(..., mode='care')`），
    与普通说话气泡一眼分开；配色仍全部来自主题 token。
  - **可点开接话**：关心气泡鼠标变手型 + tooltip，**点一下 → 展开到聊天列表并打开聊天窗**，可接着回；
    普通说话气泡点击无副作用（不把自言自语塞进记录）。本次运行第一条关心会在状态条提示一次「可点开」。
  - **留痕**：关心内容写入**回忆日志**（`memory_events`，type=`event`，标题形如「主动关心：…」），
    **不写聊天列表、不进模型上下文**——保持上下文干净的同时可事后回看。
  - **停留更久**：关心类 `max(8s, 字数×150ms+4s)`（普通类仍为 6s 起），叠加悬停暂停。
  - **扒边不再静默放弃**：先 `_popup_from_dock()` 探头把话说出来，说完由 `_hide_bubble` 自动 `_enter_dock()` 缩回。
  - **去重复**：早安回访 / 补发提醒 / 到点提醒 / 久坐提醒 / 早安问候 五处均改为只走关心气泡；
    主动关心的显示槽 `_display_wakeup` 也从 `say_plain` 改为 `say_care`。

- 新增 `tests/test_care_bubble.py`（**8 项**）：关心不入聊天列表但入回忆日志、外观含主题 accent 色条、
  停留 ≥8 秒且长于普通气泡、**点击才展开成对话**、普通气泡点击无副作用、
  **扒边时先探头且说完缩回**（回归点）、以及两条源码护栏（关心话术不得回写聊天列表；`say_care` 调用点下限）。

- **前台程序感知（v6.59，可选 · 默认关闭）**：让「主动关心」知道用户此刻在用什么程序，
  在开会/打游戏/看视频时自动让路 —— 此前 `judge_wakeup` 的提示词里写着「用户明显在忙时不打扰」，
  却**根本没有任何「忙不忙」的信号**，全靠模型猜。新增 `pet_foreground.py`（纯 ctypes、零依赖），
  三级判定：会议/游戏/视频 = 高度专注（默认不打扰）；写代码/设计、文档办公 = 专注但可打断
  （可以说话，但 message 必须更短更轻）；浏览器/终端/未知 = 不表态（完全维持原逻辑）。
  **隐私边界（5 条，有源码护栏测试）**：只读前台窗口所属进程的**可执行文件名**；不读窗口标题、
  不读页面内容、不截屏、不读输入/剪贴板；不 import 任何网络库；返回只含文件名不含路径；
  仅在用户显式勾选后生效。界面：`设置 → 通用 → 前台程序感知` —— **独立的一项，默认关闭**，
  下方常驻隐私说明与实时状态行（如「当前检测：autoclaw.exe（其他 · 不表态，按原规则）」），
  让开关的效果当场看得见。

- `care_engine.judge_wakeup()` 新增可选参数 `busy_hint`（默认 `None`，向后兼容）：传入时向提示词
  附加前台程序信号与对应的打扰规则；不传时行为与旧版逐字节一致。

- **主题 token 唯一源 `pet_theme.py`**（A1）：主题色原先**内联在主程序**，而设置窗口又另写了一套写死配色
  （「第二套主题」）——主题源不唯一，是「AI 改 UI 定位不准」的结构性原因之一。现统一为**唯一 token 源**
  （17 → **28** 个 token，新增设置窗口用色，取值与原字面量逐项一致）；主程序改为从该模块 import
  （保留 `DEFAULT_THEME` 名字，既有引用零改动）；附 `TOKEN_GROUPS` 分类索引。

- **护栏测试 `tests/test_theme_tokens.py`**（5 项）：唯一源存在、token 表合法、设置窗口零硬编码颜色、
  prompt 口径正确、**硬编码颜色只减不增**（ratchet，基线 `tests/golden/theme_hardcoded_baseline.json`）。

### 变更

- **气泡弹出机制拟人化（v6.60，批 1/4）**：此前 `say_plain` 是恒定 40ms/字的等速打字机、停留
  `max(1500, 字数×80+1000)`（约 2.3 秒）、**连续消息直接覆盖上一条**（`type_timer.stop()` 后重设文本）、
  且悬浮气泡没有 `enterEvent` → **鼠标悬停也不会暂停**。现状导致使用者反馈「太人机、关心说了等于没说」。
  本批只动显示层：
  - **节奏**：短句先说三个点（300–360ms 的「思考」停顿）→ 逐字（字速 **26–48ms 随机**）→
    标点后停顿（逗号 120ms / 句末 260ms / 换行 200ms）→ 超过 40 字**整段出现**（不再久等）。
  - **停留**：`max(6s, 字数×120ms+3s)`（关心类不再一闪而过）；**鼠标悬停暂停**（移开后 2 秒继续）。
  - **队列**：正在显示时新消息**排队**（上限 2 条，间隔 600ms 依次出现），不再互相覆盖；
    `immediate=True` 的状态/关心类同样排队，不会打断正在说的那句。
  - 参数集中在类常量区（`BUBBLE_*`），便于调；新增 `tests/test_bubble_pacing.py`（**9 项**）锁住
    「随机字速 / 标点停顿 / 最短 6 秒停留 / 排队不覆盖 / 悬停暂停」，防退化成等速打字机。

- **系统通知不再冒充桌宠说话（v6.60，批 2/4）**：此前 `_append_chat` 共 **79 处**以「桌宠」身份写消息，
  其中大部分是系统状态播报（「💾 记忆已备份」「📤 已导出 N 条」「🤖 已切换 Live2D 模型」
  「✅ 开机自启已开启」……），使用者反馈「污染会话、太人机」。本批按行号白名单逐处分类：
  - **64 处迁移**到新的 `_notify()` → **窗口底部状态条**（细条 + 主题色，默认停留 2.5 秒）+ 日志；
    **不写聊天列表、不进模型上下文**；非主线程调用自动转主线程。
  - **9 处保留**在聊天列表：首次引导语、命令结果（/time /calc /todo /help）、记忆列表内容、天气回填。
  - **1 处删除**：思考时的自言自语（`do_thinking` 的随机台词）此前也写进聊天记录，现只走气泡。
  - **5 处留给批 3**：早安回访、补发提醒、到点提醒、久坐提醒、早安问候——它们属于「关心」通道，
    将与关心气泡的独立外观一并处理（含提醒重复写两处的问题）。

- 状态条实现：`_init_status_bar` / `_style_status_bar`（配色走主题 token，**零硬编码颜色**）/
  `_place_status_bar`（贴窗口底部，随扒边变窄自动夹宽）/ `_notify`；
  控件设 `WA_TransparentForMouseEvents`，不遮挡立绘点击；不参与布局（避免挤压立绘导致上下跳动）。

- 新增 `tests/test_status_bar.py`（**8 项**）：状态条默认隐藏、**状态提示不得写进聊天列表**
  （污染回归点）、对话内容仍进聊天列表（对照）、自动隐藏计时、空文本忽略、
  配色必须取自主题 token、以及两条源码护栏（系统话术不得回写聊天列表——按关键词扫描；
  `_notify` 调用点数量下限——防迁移被悄悄回退）。

- system prompt「桌宠自身能力」章节改为**诚实口径**：颜色**优先走主题**，唯一 token 源是 `pet_theme.py`；
  **只有该文件确实没有对应 token 时**（小游戏/关系面板/卡片等尚未主题化的部位）才允许改源码，
  且必须走 `search_code → read_file(带行号) → edit_own_code(按行)`，并在回复里报出改了哪个文件哪几行。
  （此前提示词声称「UI 样式（……**所有**颜色）都在主题系统里」，与事实不符——实际约 145 处颜色在主题系统之外。）

- **小游戏（15 款）接入主题**（A2-1）：`pet_minigames.py` 原有 **55 处**硬编码颜色、完全不随主题变化
  （换浅色主题后只有聊天面板变了，一进小游戏还是深色）。现改为**模块级主题快照 + 取色函数 `T()`**，
  子控件挂样式工厂（`_theme_qss`）以便切主题时统一重刷；2048 色阶 / 扫雷数字色 / 俄罗斯方块方块色 /
  西蒙记忆色板改为 **token 映射**。`pet_theme.py` 新增 **34 个 `mg_*` token**（取值与原字面量逐项一致）。
  宿主在 `_apply_theme()` 里推主题给小游戏模块、开窗前先同步主题。详见 `docs/主题token化-A2-1-小游戏-20260915.md`。

- **主程序 / 卡片 / 关系面板接入主题（A2-2）+ 生效主题中央通道**：`pet_theme` 新增
  `active()/color()/set_active()/subscribe()` —— **当前生效值只有一处**，各 UI 模块只取色 + 登记
  “变化时怎么重刷”，不再各自存一份；宿主 `_apply_theme()` 末尾广播给所有订阅者。
  覆盖 `desktop_pet.py`（41：任务侧栏/监控悬浮窗/附件卡片/选项按钮/流式与思考区/费用气泡…）、
  `chat_cards.py`（11）、`affection_ui.py`（17，含新建主题通道）、其它 13。
  token 表 79 个（A2-1 的 `mg_*` 正名为通用 `ui_*`）。**至此产品代码硬编码颜色 = 0**（172 → 144 → 89 → **0**）。
  详见 `docs/主题token化-A2-2-主程序与卡片-20260915.md`。

### 修复

- **小游戏界面空白**（使用者实测「游戏界面没了」）：A2-1 主题化迁移时，替换 import 块的锚点把
  `from PySide6.QtGui import QPainter, QPen, QColor, QBrush` **整行吃掉了** → 所有画布类游戏
  （五子棋 / 扫雷 / 贪吃蛇 / 2048 / 俄罗斯方块…）的 `paintEvent` 抛 `NameError`，对话框能打开但**画面全白**。
  已补回 import。

- **教训已固化为测试**：新增 `tests/test_minigames_paint.py` 与 `tests/test_ui_paint_sweep.py` ——
  对每个 UI 面 **真 show + processEvents + grab**（真正触发 paintEvent），并捕获 excepthook / stderr 异常；
  覆盖主窗口 / 设置窗 / 模型管理窗 / 关系面板 / 回忆相册 / 小游戏列表页 / 15 款游戏 / 代码卡 / 表格卡。
  此前所有断言都只查“能构造、样式表有色”，**抓不到绘制期错误**——这正是该 bug 能潜过的原因。

- **「AI 装了主题却切不生效」**（使用者实测：桌宠报告已安装并切换主题，界面毫无变化，重启也无效）。三个真 bug：
  1. **GUI 刷新投递错了**：`_tool_set_theme` 用 `QTimer.singleShot(0, self._apply_theme)` 从工作线程「回主线程」，
     但 AI 工具跑在工作线程（`Thread`）里、**该线程没有事件循环 → 定时器永不触发**；结果主题数据改了、
     `config.json` 也写了，界面却从未刷新。改为跨线程信号 `ui_call_signal` + 统一入口 `_run_on_ui()`（项目既有做法）。
     同类问题一并修：`_write_config_tool`（切语言 / 显示模式）、`say_plain`、`_append_chat`、`_append_chat_md`。
  2. **启动不恢复主题**：配置读取那一层只写不读 `theme` → 重启即被静默重置为 `default`。
     现由 `_load_ai_config` 记住、`_init_finish` 恢复（**早于历史回显**，回显出的气泡即用主题色）。
  3. **旧消息不换色**：气泡配色在创建时就写进 `QLabel` 样式表 → 切主题后历史消息仍是旧色。
     现登记受主题着色的控件（`_theme_widgets`）并在 `_apply_theme()` 里统一刷新（已销毁控件自动剔除）。

- `pet_bubble` 新增 `message_label_qss()` / `apply_message_label_theme()`：消息气泡配色**单一来源**，
  创建与刷新共用同一规则（避免两处写法漂移）；兜底色改从 `pet_theme` 取，不再写死。

- **工具梯级把「用户能感知的能力」收得太狠**：`read_file` / `write_file` / `search_code` / `edit_own_code` /
  `install_plugin` / `uninstall_plugin` / `list_plugins` / `set_theme` / `skill_run` 原先都在「进阶工具模式」里，
  导致使用者让桌宠“装个主题并切换”时 AI 手上没有工具，只能回“工具没装上”。
  现把这 9 个补回核心（核心 8 → **17** 个；默认模式约 7.5K 字符 ≈3.8K token，仍比全量省 **32%**）；
  进阶模式保留偏系统/偏危险的工具（PowerShell / 进程 / 剪贴板等）。

- **系统提示词补一条**：需要的能力确实不在工具列表时，明确告诉用户去 设置 →「放开全部工具（进阶模式）」，
  不许谎称已完成，也不许说“工具没装上”这种含糊说法。

- **代码卡长行被右缘裁掉**：`CodeCard` 内部原为 `QTextEdit.NoWrap`（横向滚动），实测一条 JSON 代码块
  文档宽 558px、卡片可视宽 408px → **约 27% 内容看不见**。改为 `WidgetWidth`（长行换行），
  不再有被裁的字；「复制」按钮仍复制原文。

- **「自改代码（自我进化）」在真实环境中几乎必然失败**（使用者反馈「几乎没成功过」）。定位到四个根因：
  1. **行尾不匹配**：项目内文件行尾不统一（`desktop_pet.py` 为 LF，`memory_engine.py` / `tools_registry.py` 为 CRLF），
     而 AI 经 JSON 传来的 `old_text` 一律是 LF → 精确匹配**必然失败**。改为**按行归一化后匹配连续行块**（同时容忍行尾空格差异）。
  2. **编辑会污染行尾**：`splitlines()` + `'\n'.join()` 把整个文件行尾改写成 LF（实测 CRLF 444 → 0，git diff 爆炸）。
     改为**按原文件行尾写回**。
  3. **改前备份一直是坏的**：`datetime.datetime.now()` 但模块未 `import datetime` → `NameError` 被静默吞掉，
     `backup/` 始终为空，而返回消息却提示「可用 backup 恢复」。现在真正写备份，且**只在成功时提及**。
  4. **打包版必然失败**：语法校验走 `subprocess.run([sys.executable, '-c', ...])`，冻结后 `sys.executable` 就是桌宠 exe；
     且打包目录没有 `.git`。改为**进程内 `ast.parse`**，并在冻结环境给出明确原因提示。

- `search_code` 支持递归子目录（此前只扫顶层，模块搬进子目录后搜不到）。

- 自改链路的失败路径全部接入日志（替换 4 处静默 `except: pass`）；成功消息带上**文件名与行号**。

### 测试

- 单测总数 **107 → 116**（全绿）。

- 单测总数 **132 → 141**（全绿）。

- 单测总数 **124 → 132**（全绿）。

- 单测总数 **116 → 124**（全绿）。

- 新增 `tests/test_foreground_privacy.py`（**9 项**）：源码护栏（无窗口标题 API / 无截屏与输入类 API /
  无网络调用）、返回值不含路径、**默认值必须为 False**、七类进程分类与三级忙碌判定、
  `busy_hint` 形态（空输入必须为空、不得出现盘符路径与目录名）、隐私说明措辞覆盖四项承诺、
  `judge_wakeup` 必须接受 `busy_hint` 且默认 None。

- 单测总数 **98 → 107**（全绿），其中包含既有的「真绘制」扫荡与主题护栏。

- 单测总数 **67 → 81**（本轮新增 14 项）：
  - `tests/test_theme_tokens.py`（5 项）：唯一源存在、token 表合法、设置窗口零硬编码颜色、prompt 口径正确、硬编码颜色只减不增；
  - `tests/test_theme_persist.py`（6 项）：插件主题变量读取、**工作线程切主题必须真刷新面板**（老 bug 复现点）、
    启动恢复主题、未知主题安全回退、切主题刷新历史气泡、源码护栏（禁用 QTimer 投递 / 启动必须接恢复）；
  - `tests/test_code_card_wrap.py`（2 项）：代码卡按宽度换行、放进 360px 容器不溢出；
  - `tests/test_tool_tiers.py` 增 1 项：**外观/自改/插件/文件类工具必须在默认集合**（锁定本次修复）。

- 主题颜色护栏改为**只扫产品代码**（`tests/` 夹具需要写字面量来断言颜色）。

- 端到端验证：以真实 `config.json` 离屏构造桌宠 → `current_theme` 与主题字典正确、面板样式表已含目标色、
  默认色不再出现；`golden_ui` 的 theme 分区随之同步（插件内容变化后重采）。

- 主题 token 化 A1 的验证证据：面板样式原文未动、原 17 token 取值逐字节一致、设置窗口 QSS 渲染逐字节一致、
  黄金基线仅 `theme` 分区变化（17→28 只增未删，逐帧像素等 6 个分区未变）。详见 `docs/主题token化-A1-20260915.md`。

- 新增 `tests/test_minigames_theme.py`（5 项）：无残留字面量 / `T()` 跟随主题 / **15 款游戏全部可构造且无缺键兜底色** /
  已开窗口切主题自动重刷 / 小游戏列表页接入主题。单测总数 **81 → 86**。

- 颜色护栏（ratchet）基线：**144 → 89 处**（小游戏清零）。

- **值等价性机器证明**：旧 `pet_minigames.py` 里 36 种颜色字面量全部能在 token 表找到同值 → 默认主题渲染不变。

- 新增 `tests/test_host_theme.py`（5 项）、「真绘制」扫荡 `tests/test_minigames_paint.py`（2）+
  `tests/test_ui_paint_sweep.py`（3）；单测总数 **86 → 98**。

- 新增 `tests/test_selfcode_success.py`：**8 项成功路径断言**（CRLF 文件 + LF 文本匹配、行尾保留、
  备份落盘、行尾空格容忍、语法门仍生效、多处匹配给建议、消息可用性、源码护栏）。
  此前 `test_pet_selfcode.py` 的 12 组断言**全是拒绝分支**，成功路径 0 覆盖 —— 这正是问题能长期潜伏的原因。

- 单测总数 **57 → 67**（成功路径断言 8 → 10）。

- **行内片段匹配回退**：整行/连续行块匹配不到时，再按「归一化后的行内子串」匹配
  （保留旧语义——实测模型经常只给一句话中间的片段）。匹配**成功后才写备份**，失败不留噪音。

- `tools_registry.py` 中 `edit_own_code` 的工具描述同步更新为现状（「改前记录基线 hash」而非「改前提交基线」）——
  这条描述是模型可见的，过时描述会误导它。

### 真实性测试（v6.56b）

用**真实链路**跑了两轮端到端（真实 system prompt + 真实工具 schema + 真实 API + 真实分发）：
| | 第一轮（修前） | 第二轮（修后） |
|---|---|---|
| 轮次 | 6 | **5** |
| 首次编辑 | 失败（行内片段匹配不到）→ 改用行号模式才成功 | **一次成功** |
| 备份噪音 | 2 份（失败也留） | **1 份** |
| 结果 | 内容/行尾/语法正常 | 内容/行尾/语法正常 |
模型在无人干预下自行完成：`search_code` → `read_file` → `edit_own_code` → 回读确认 → 汇报，
并主动说明「需重启生效」与「改前备份位置」。

## [2.7.1] - 2026-09-14

流式取消、记忆过拟合治理与工具梯级暴露（基于用户反馈的 5 个现象排查）。

### 修复

- **`/stop` 停不了、还在继续吐字**：流式循环原先只在“工具轮次边界”查任务代次，流内部零检查，
  且网络层没有取消入口。现在**逐块检查取消回调**（`stream_chat_completions(should_cancel=...)`），
  命中即断开连接——真停，同时**停止计费**。
- **按下停止后“放飞 / 打转”**：`_stop_ai()` 原先会立刻启动队列下一个任务，与尚未结束的旧字流交错输出。
  现在改为**等旧线程退出后**由 worker 收尾接续队列（`_pending_resume`）。
- **“输出上限”自定义后界面不变、也不生效**：设置窗口「自定义…」写的是 `config.json`，
  而发请求与界面回显读的是**模型档案** `models.json`。已统一改走档案，并在保存后回填设置窗口。
- **思考“有时会自己停”**：被新任务顶替时在下一边界**静默 return**（不报错不留痕）→ 现写入日志
  （`_log_ai_abort`，含代次与阶段）。
- **兜底路径截断**：空回复重试 / 结果整理两条兜底路径原先将 `max_tokens` 写死为 500 / 300
  → 改为 `min(4096, max_tokens)`。
- **表格 HTML 双重转义**：`md_to_html` 入口已整体转义，`md_table` 又转义一次，
  导致单元格里的 `<` / `&` 显示为 `&amp;lt;`。已加 `escape` 参数，两条调用路径行为均正确。
- **3 处非原子写**（导出聊天记录 / 存档 / 记忆备份）改为原子写；
  「存档并清空」额外**校验写盘结果**——存档文件不存在或为空时取消清空对话。
- **冻结版日志写不出来**：Pack 后 `pet_log` 按 `__file__` 定位落在包内，且初始化失败被静默吞。
  现在 `sys.frozen` 时日志落到 **exe 同级 `logs/`**。

### 变更

- **记忆过拟合治理**（用户反馈“问什么都往已记住的事上靠”）：
  - 检索新增**相关性闸门**：命中≥ 2 个查询词或覆盖率 ≥ 34%（且 BM25 ≥ 0.25×最佳）才注入；
    **未过闸 → 本轮不注入检索记忆**；
  - 注入块写死使用约束：“记忆只作背景，仅当问题直接相关时才引用；不要主动把话题引向记忆”；
  - 默认 `search_memory()` 行为保持兼容（闸门仅作用于注入路径）。
- **工具梯级暴露**：默认只给 8 个陪伴/日常高频工具（时间/天气/提醒/待办/记忆/情感选择支/主动回访/联网搜索），
  其余收进设置窗口「进阶模式」一键恢复；MCP 与插件工具不受影响。
  **实测工具 schema：11,115 → 3,242 字符（≈5.6K → 1.6K token，省 71%）**。

### 工程

- 新增 4 个测试文件：`tests/test_stream_cancel.py`（流式取消，假 SSE 验证）、
  `tests/test_memory_gate.py`（闸门）、`tests/test_companion_smoke.py`（陪伴核心 11 项）、
  `tests/test_tool_tiers.py`（梯级暴露）——**单测 33 → 50 项**。
- 补上**陪伴核心最小回归**（好感度 / 回忆日志 / 气泡渲染 / 主动关心）：
  此前 31 个模块中 13 个零测试，且盲区恰好集中在陪伴体验。
- 回归：40 项回归全过、50 项单测全过、UI 黄金基线完全一致；
  首次完整打包验收（PyInstaller 6.21 / Python 3.14），冻结版启动实测两次无崩溃。

## [2.7.0] - 2026-09-13

结构性重构与测试网建设（基于 09-12 全项目审查的后续收尾）。**不新增功能，也不改变任何交互行为**，
换来的是：宿主类净减 151 行、7 套自动化验证（含 UI 黄金基线）、以及若干处能直接感知的体验修正。

### 修复

- **输入框文字色纳入主题 token**：`_panel_qss` 里写死的 `color:#fff` 改为读 `input_text`，
  浅色主题下输入的文字不再看不见（这是全盘 221 处硬编码色值里唯一真正的主题盲区）。
- 消息气泡字号 12px → 13px（与顶部说话气泡对齐）。
- 小游戏菜单图标去重：`🔢`×3 / `⚫`×2 / `🃏`×2 → 拆分后 15 款各不相同。
- 小游戏存档改原子写；记忆时效加权（`score_recency`）从死代码真正接进排序。
- 流式重试不再重复回显；10 处静默异常接入日志。
- 设置窗口两处控件不回填（报告 6.3 项）。

### 变更

- **小游戏窗口最小尺寸显式化（原描述已复核修正）**：修前 `BaseGame.showEvent` 未设最小尺寸、
  `minimumHeight` 属性为 0，但这**不等于**「窗口能被拖成 0 高」——Qt 会在窗口显示时按布局设置最小尺寸。
  对照实验（同一款游戏分别用旧/新 `showEvent`，再尝试把窗口压到 60 px 高）显示：**修复前后可缩下限一致**
  （如五子棋都是 430 px）。本改动的作用是把最小尺寸**显式抬到布局建议尺寸**（并让最小宽度跟随
  `sizeHint`），同时给后续改造留下一条可测契约（冒烟测试新增断言：显示后 `min ≥ sizeHint`）。
- **体积**：立绘按显示尺寸压缩 **248 MB → 93.5 MB**（长边 2048/2476 → 1024）；
  仓库同时从 8.71 GiB 清理到 223 MiB。
- **工具分发注册表化**：`_execute_tool` 从 169 行 / 29 个分支 变为 16 行 + 29 个小方法
  （新增 `tools_registry.py` / `tools_executor.py`）。
- **小游戏基类上收**：`over` 属性收敛到 `BaseGame.__init__`（9 处重复声明删除）。

### 工程

- **宿主类持续变薄**：`PetWidget` 6,199 → **6,048 行**（净减 151）。
  - 文档/OCR 解析 → `pet_docs.py`（−117 行）
  - 自身代码读写 → `pet_selfcode.py`（−127 行，含一次 revert 重做 + 两道写盘自检）
  - 气泡与 Markdown 渲染装配层 → `pet_bubble.py`（−39 行）
  - 状态机与动画（低耦合段）→ `pet_anim.py`（−91 行）
- **回归测试与私有属性解耦（#18）**：新增 `snapshot()` / `set_state()` / `ui_probe()` 等 5 个窄接口，
  `regression_test.py` 里 48 处私有属性直读全部改到接口——这是后续搬模块能安全进行的前提。
- **新增自动化验证（7 套）**：
  - `tests/golden_ui.py` **UI 黄金基线**：7 个分区指纹（markdown 渲染 / 立绘资产 / 逐帧像素 /
    状态语义 / 主题与样式表 / ui_probe / 几何），`capture` 与 `check` 双模式，后续 UI 搬迁可机器对照
  - `tests/test_pet_sysutils.py`：剪贴板 / `run_ps` / 音量 / 热键 15 组断言（全打桩）
  - `tests/test_minigames_smoke.py`：**15 款小游戏冒烟**——构造 → 防裁切 → 遍历难度 → 重开局
    → 存档往返 → 结算回调（此前零覆盖）
  - `tests/test_pet_selfcode.py`：自身代码读写 12 组断言
  - 以及既有的 `test_command_gate.py`（105 断言）/ `test_tool_registry.py` / `test_pet_docs.py` / `test_model_registry.py`
- **未做并附理由**：长历史「虚拟化」——实测已有分页机制（每次 30 条 + 手动加载更多 + 上限 300 条），
  虚拟化无用户可见收益；扒边/窗口几何的模块抽取推迟到有真实需求时再做（依赖真实屏幕边缘、多显示器、DPI，
  离屏环境测不了）。

## [2.6.0] - 2026-09-12

安全加固与健壮性修复（基于全局代码审查）——重点修掉「AI 能执行的命令基本没设防」
和「配置文件写坏就起不来」两类问题。**不改变任何功能行为，只把口子堵上。**

### 安全

- **命令安全门重写**：`check_dangerous` 从「危险关键词黑名单」改为
  「破坏性硬拒绝 + 只读白名单 + 默认拒绝」三层判定。旧黑名单实测放行
  `Stop-Computer` / `rd /s /q` / `cmd /c del` / `icacls` / `taskkill /f`，
  还误拦 `Remove-Item -Recurse -Force`；新规则已用 105 项断言固定（`tests/test_command_gate.py`）。
- **表达式计算去掉裸 eval**：`calculate_expr` 改 AST 白名单求值，
  并加长度 / 幂次 / 位宽上限（原先 `9**9**9` 会把进程算到卡死）。
- **命令拼接不再裸拼字符串**：`open_app` 的 8 处 `os.system('start ...')` 改为
  `os.startfile` / `webbrowser.open`（URL 仅允许 http/https）；
  `search_files` 的路径与关键字改用 PowerShell 单引号转义；
  `kill_process` 的进程名加白名单校验；自启快捷方式脚本的三个插值变量同样转义。
- **聊天表格渲染补 HTML 转义**：`chat_render.md_table` 原先把单元格原文直插
  `<td>`，AI 回复或文件内容里的 `<img src="file:///...">` 会被当富文本渲染。

### 修复

- `models.json` 里写一个 `temperature: "abc"` 曾让桌宠起不来（裸 `float()`）：
  改容错取数 + 温度夹到 0.0~2.0 + 非法价格丢弃；单条档案损坏不再拖垮启动。
- `role_anchor`（"回答你是谁"的角色规则）在 `build_system_prompt` 里算出来却从未拼进
  prompt，现已接上（system prompt 3073 → 3200 字符）。
- 小游戏「💾 保存」按钮曾是假按钮：15 款里只有扫雷实现了存档，其余点击只弹
  「该游戏暂不支持保存进度」。现在只有真正支持存档的游戏才显示该按钮。
- 连续粘贴多张截图会互相覆盖（固定文件名 `_pasted_ocr.png`）→ 文件名加毫秒时间戳，
  清理逻辑改通配匹配。

### 变更

- `pyproject.toml` 版本号 `1.0.0` → `2.5.0`（此前落后 CHANGELOG 十个版本）；
  补齐 `Pillow` / `pypdf` / `mcp` 的依赖声明（代码里是懒加载，声明此前缺失）。

## [2.5.0] - 2026-09-12

设置整合与右键菜单精简（Phase 5）：把「动作」和「配置」分开。

### 新增
- **统一设置窗口**（`settings_ui.py`，入口：右键菜单 → ⚙️ 设置…）：左侧六个分类——通用 / 对话 / 外观 / 模型与 API / 记忆与数据 / 系统——把原先散在「设置」子菜单里的 40+ 项配置收拢到一处。所有改动**立即生效**（复用已有的 setter 与热加载，不新增暂存/提交逻辑）
- 回归新增 H21（设置窗口六分类 / 控件回填 / 改动回写 / 按钮齐备），共 39 项

### 变更
- **右键菜单重构**：一级从 **10 项压到 6 项**；可达叶子条目约 **75 → 40**；最深层级 **3 → 2 级**
  - 新增「🎭 形象」：合并原「角色」「性格切换」与立绘模式
  - 新增「📊 状态」：合并原「关系」与「工具」里的统计项（统计悬浮窗 / 按模型统计 / 查看统计历史）
  - 「⚙️ 设置…」改为打开统一设置窗口，替掉原来的 40+ 项子菜单
  - 「💬 互动」瘦身：场景动作由「常用 5 + 更多动作 7」两级嵌套合并为一层 12 项；Live2D 调试窗口与「存档并清空对话」移入设置
  - 「🔧 工具」子菜单取消：联网搜索 Key 进设置、统计进状态
  - 插件菜单项收进「🔌 插件」（仅在装了 menu 类插件时出现）
- 托盘菜单的「角色」子项由模型档案驱动（与右键保持一致）

> 功能一个不删：原先设置子菜单里的每一项在设置窗口里都能找到。

## [2.4.0] - 2026-09-12

第四轮：按模型统计、字段校验、每档案独立 key（多服务商 / 中转）。

### 新增
- **按模型统计**（右键菜单 → 🔧 工具 → 📈 按模型统计）：终身口径按模型汇总次数 / token / 费用，费用从高到低排序；档案里没配价格的模型单独标「⚠ 其中 N 次价格未知」。累计随 `api_stats.json` 持久化
- **档案字段校验**（`model_registry.validate_key` / `validate_profile_fields`，界面与测试共用同一套规则）：
  - 内部键：1~32 位字母 / 数字 / 下划线 / 连字符（中文、空格、超长一律拒绝）
  - 显示名与模型 ID 非空；接口地址必须以 `http://` 或 `https://` 开头
  - 主题色必须 `#RRGGBB` 或 `#RGB`；温度 0.0~2.0；输出上限 256~384000；价格不可为负
  - 非法值不再被静默改成默认值，而是逐条列出让你改
- **每份档案可用不同的 API Key 字段**：`api_key_field` 从「只存不用」变成真正生效——所有请求路径统一走 `_current_api_key()`，按当前档案的字段名从 `config.json` 取 key，未配置则回退主 key。于是同一台机器可以一份档案挂官方、另一份档案挂中转
- 回归新增 H18（字段校验 + 界面拦截）、H19（按模型统计）、H20（每档案独立 key），共 38 项

### 修复
- 修正错字：`兜底` 被误写成 `兵底`（api_stats / deepseek_client / desktop_pet / regression_test / README / CHANGELOG 共 8 处）；另在批量替换时误伤过 `主动消息机制学习笔记.md` 里的「闹钟哨兵」，已还原

## [2.3.0] - 2026-09-12

收尾（Phase 3）：价格未知提示上浮、档案可导出/导入、清掉已迁走的旧配置键。

### 新增
- **模型档案导出 / 导入**（模型管理对话框底部两个按钮）：导出成可分享的 JSON，**不含任何密钥**（只留 `api_key_field` 字段名）；导入支持「合并」（同名覆盖、其余保留）与「整体替换」两种方式，坏文件被拒且不影响现有档案
- **价格未知显式提示**：API 统计悬浮窗的「最近」一行、以及「查看统计历史」的每条记录，遇到档案里没配价格的模型会标出 ⚠（原先静默按兜底价算）
- 回归新增 H15（导出/导入）、H16（价格未知提示）、H17（清键后仍产出厂规范 ID），共 35 项

### 变更
- **清理 config.json 里的旧键**：`deepseek_model`（代码从未读过）、`model_flash` / `model_pro`（迁移来源，models.json 已在则无意义）已移除；`config.example.json` 同步去掉已迁往 models.json 的 `model_flash`/`model_pro`/`reasoning`/`temperature`/`max_tokens`
- 旧版配置仍可迁移：首次生成 models.json 时照旧读取 `model_flash`/`model_pro`（没有则用出厂默认）

## [2.2.0] - 2026-09-12

模型管理界面（Phase 2）：档案可增删改，并能一键拉取官方模型列表、做连通性自检。

### 新增
- **模型管理对话框**（`model_manager_ui.py`）：右键菜单 → ⚙️ 设置 → 🎯 模型管理…
  - 档案列表 + 新增 / 复制 / 删除（至少保留一份）
  - 逐项编辑：显示名、模型 ID、接口地址、副标题、主题色、温度、输出上限、思考开关、价格（输入/缓存/输出）
  - **拉取官方模型列表**：`GET /models`，下拉选择直接填进「模型 ID」，不用手打
  - **连通性自检**：发 `max_tokens=1` 的最小请求，回显「响应里的真实模型 + 耗时」；若官方已把请求的 ID 指向别的模型会明确提示
  - 恢复出厂（当前档案）
- **思考模式 / 采样温度进右键菜单**：原先只从 config.json 读、菜单里没有入口；现写入当前角色的模型档案（热加载生效）
- `deepseek_client.models_url / list_models / probe_model`：模型列表与连通性探测的网络层（地址由档案 endpoint 推导，换中转/代理时跟着走）
- 回归新增 H13（对话框保存/恢复出厂/增删/防删空）与 H14（模型列表地址推导），共 32 项

### 变更
- 「⚙️ 设置 → 🎯 角色模型」子菜单（逐角色改 ID）由统一的「🎯 模型管理…」入口取代

## [2.1.0] - 2026-09-11

模型身份配置化：把写死在代码里的模型 ID / 显示名 / 接口地址 / 参数 / 价格
收敛成一份用户可编辑的模型档案。

### 新增
- **模型档案 `models.json`**（`model_registry.py`）：每个模型一条档案，含显示名、模型 ID、别名、接口地址、温度/输出上限/思考开关、价格、外观与人设。首次运行自动生成，并从旧 `config.json` 的 `model_flash`/`model_pro`/`reasoning`/`temperature`/`max_tokens` 迁移
- **模型身份单一来源**：`CHARACTERS` 改为运行时从档案构建（结构与旧字典完全兼容，下游 15 处用法无需改动）；右键菜单/托盘的“角色”与“模型”列表也由档案生成——**加一条档案就多一个角色，不用改代码**
- **接口地址统一**：原先 `deepseek_client.py` / `care_engine.py` / `desktop_pet.py` 三处各写一份 URL，现均从档案的 `endpoint` 取（全项目只剩一个默认值定义）
- **价格未知显式标记**：查不到价格的模型在统计条目里带 `price_unknown`，不再静默套用兜底价
- `tests/test_model_registry.py`：档案注册表 18 项单测（首次生成 / 迁移 / 改名升级 / 损坏回退 / 价格 / 兼容层 / 增删 / 落盘往返）
- `models.json.example`：档案模板（供开源使用者参考）

### 修复
- **滚动摘要从来不执行**（`_summarize_old`）：函数内引用了另一个作用域才有的 `cur_model`，抛出的 `NameError` 被 `except Exception: pass` 吞掉——导致对话摘要从不生成、最旧 10 条从不裁剪、上下文无限增长。改为复用 `_extract_chat`，顺带少一份裸写 URL
- **Pro 用量按 Flash 价计费**：流式路径记录用量时未传模型名（`model=''`），一律落到兜底价；两者单价差 3 倍。补上 `fallback_model`
- **价格表因官方重命名失效**：官方将 `deepseek-v4-flash` 重命名为 `deepseek-flash` 后，响应的 `model` 字段与请求写的 ID 不一致，而价格表以请求 ID 为键 → 查不到、静默落兜底价。现按模型档案的规范 ID + 别名归一化后查询
- **输出上限三处互相矛盾**：读取时夾 64000、菜单写 128000、AI 工具写 128000，导致“选 128000 → 存进去 → 重载被压回 64000”，菜单勾选态也对不上。统一为 `model_registry.MAX_OUTPUT_TOKENS`（384000，对齐官方输出上限）

### 变更
- 模型设置弹窗改为写模型档案（不再往 `config.json` 写重复字段），菜单名改按档案显示名展示
- `reasoning`（思考开关）/ `temperature`（采样温度）改由档案提供（原先只从 config.json 读、右键菜单无入口）
- `.gitignore` 新增 `models.json`（本机配置，含个人模型/接口设置）

## [2.0.0] - 2026-08-29

重大版本：好感度与成长系统 + 15 款小游戏大厅 + 动作素材扩充。

### 新增（好感度与成长系统）
- **好感度引擎**（`affection_engine.py`）：双轴养成——好感度 0~200（情感亲密度）+ 等级/XP（陪伴资历）；事件驱动纯积累制（对话/任务/喂食/小游戏/早晚安/陪伴时长），零衰减零惩罚，三重防刷（冷却/单日上限/封顶）；6 称号 + XP 曲线；JSON 原子持久化
- **五阶段人设进化**：初见→熟悉→亲密→依赖→灵魂伴侣，system prompt 随好感度动态注入（称呼/语气/关心强度逐级进化，双角色独立）
- **三层记忆**：新增事件记忆层（`memory_events.py` 回忆日志，200 条滚动，prompt 注入最近 3 条）
- **关系面板**（`affection_ui.py`）：好感度进度条 + 阶段徽章 + 等级/XP + 称号 + 统计
- **回忆相册**：按时间线浏览共同经历（里程碑/事件/标记，含当时好感度）
- **情感选项（galgame 选择支）**：AI 用 `offer_choices` 工具在抉择时刻给出 2~3 个可点击选项，支持 {text, affect} 情感标签（❤️+n），点击即发送并加好感；选择后按钮禁用防重复
- **余额动画气泡**：API 费用角色头顶上浮渐隐（>¥0.1 红色）
- **饱食度系统**：时间戳衰减，喂食恢复 + 好感（冷却 30 分钟），低饱食只提示不惩罚
- **高分里程碑**：每款游戏独立最高分，破纪录 → 桌宠庆祝 + 回忆日志 + 额外好感

### 新增（小游戏大厅，7 → 15 款）
- `pet_minigames.py` 注册式框架：BaseGame 统一难度选择 + 结果带分回调 + 桌宠表情区 + 规则说明按钮
- **新增 8 款**：井字棋（三档 AI）/ Farkle 骰子（天国拯救同款，目标分 500~10000 自选）/ 打地鼠 / 21 点 / 数独 / 俄罗斯方块 / 华容道 / 西蒙记忆
- **已有 7 款加难度/玩法**：五子棋三档 AI、2048 多档（4×4/5×5/4096）、扫雷经典三档、贪吃蛇速度三档+黄金食物、记忆翻牌 6/8/12 对、猜数字范围/限次三档、石头剪刀布三局两胜+桌宠记仇
- **Farkle 计分对齐 KCD 官方**：顺子 123456=1500 / 12345=500 / 23456=750 / 四五六同 ×2/×4/×8 / 单1=100 单5=50（取消三对子/双三同变种奖励）
- 全部游戏带 📖 规则说明 + 难度可选 + 桌宠表情实时反应

### 新增（动作素材）
- 12 个新动作状态（饿/胜利/亲亲/沮丧/跳舞/唱歌/大哭/惊讶/得意/求摸头/撒娇抱抱/送花），双角色齐全（每角色 37 状态），asset() 缺失自动兜底
- 触发联动：饱食度低→饥饿图；小游戏胜/负→欢呼/沮丧图；喂食→亲亲图
- 互动菜单精简：常用 5 动作直显，其余收「🎬 更多动作」子菜单

### 修复
- 贴边状态疯狂左右抽动（animate 贴边定位与摆头 move 冲突）
- 扫雷棋盘空白（QRect 未导入致绘制崩溃）、2048 移动方向反转
- 情感选项可重复点击发送（点击后整组禁用）
- Farkle 无点选交互重做（点击骰子选中/保留选中/全保留重掷/锁定）+ 计分重复 bug
- 21 点自动发牌 + 牌面 str 转换崩溃
- 游戏窗口关闭后定时器泄漏（后台继续跑/突弹结算窗）+ singleShot 回调防 GC
- 键盘游戏键位冲突（难度下拉抢焦点，↑↓切难度/空格弹下拉 → showEvent 统一抢回焦点）

### 工程
- 好感度/回忆/API 统计等运行数据加入 .gitignore；好感度与成长系统/小游戏扩展设计方案文档入库

## [1.1.1] - 2026-08-06

数据安全 + AI 请求健壮性小版本（基于全栈开发知识调研落地）。

### 修复（数据安全）
- **JSON 原子写保护**：所有持久化文件（聊天记忆/事实记忆/待办/提醒/config/应用索引）改为「写 .tmp 临时文件 → os.replace 原子替换」，程序崩溃/断电不再损坏原文件（原直接 open('w') 会截断内容，损坏后静默丢失数据）
- 新增 _atomic_write_json 统一入口，8 个保存点全部切换

### 改进（AI 请求）
- **指数退避重试**：网络/服务繁忙重试等待从固定 5 秒改为 5 秒→10 秒（第 1 次 5 秒、第 2 次 10 秒），状态栏提示具体等待秒数与第几次重试

### 说明
- 数据量评估后暂不迁移 SQLite（聊天记忆 50+300 条上限、最大文件约 127KB，JSON 量级可控；迁移动面大收益低），等聊天记录全量持久化需求出现再评估

## [1.1.0] - 2026-08-04

重大更新：AI 自我管理 + 联网 + 完整附件系统。

### 新增（AI 能力）
- **联网搜索**：web_search 工具（Tavily API，免费 1000 次/月），AI 知识截止 2024.8 后自动联网查最新信息；设置菜单可配 key
- **AI 自我管理三阶段**：read_file（读自己代码自查）/ write_config（白名单改配置热加载）/ edit_own_code（git 保护改自己代码，语法验证+失败不落盘）
- **write_file**：AI 主动生成文件（统一写入 输出/ 目录，防路径穿越）
- **Token 上限扩展**：档位至 128000（对齐 DeepSeek 输出上限 384K）
- **API 自动重试**：503/429/5xx 等 5 秒重试 2 次，状态栏提示

### 新增（会话与附件）
- **会话管理重做**：显示历史与 LLM 上下文分离持久化，重启回显 30 条 + "显示更多"分批加载
- **提醒持久化**：关机不丢，开机补发错过提醒/回访到聊天面板
- **导出聊天**：可视化勾选要导出的消息 + 可选保存位置 + 默认 聊天记录/ 文件夹
- **统一附件系统**：📎 按钮/拖放/Ctrl+V 粘贴全部先暂存为附件卡片栏（图标+文件名+大小+✕），Enter 一起发送
- **常用文件识别**：txt/md/xml/svg/代码类直接读；docx(标准库 zip+XML)/pdf(pypdf)/xlsx/pptx 提取文本；图片 OCR
- **OCR 截图快捷键**：Ctrl+Alt+D 全屏截图→识别→AI 分析；截图/文件卡片化显示（不刷长串文字）

### 修复
- 聊天面板超屏（输入框被吞）；气泡被人物遮挡；随机动作重复；睡觉状态机完整修复（情绪/贴边/切角色）
- 导出黑箱（改为显示历史导出）；回显 bug（历史从未真正回显）；AI 回复不进显示历史
- 闭眼贴边图全套（Seedream 生成 + 连通域保护抠图）
- 自启动改快捷方式形态；启动实测 2.1s

---

## [1.0.1] - 2026-08-03

工程化与文档完善（无功能变更）：

### 新增
- `requirements.txt` / `pyproject.toml`：依赖声明与项目元数据
- `tests/smoke_test.py`：冒烟测试（导入/构造/核心函数，6 项通过）
- `CONTRIBUTING.md`：贡献指南（含源码阅读地图）
- `SECURITY.md` / `CODE_OF_CONDUCT.md` / `.editorconfig`
- `.github/`：Issue 模板（Bug/Feature）+ PR 模板
- README 中英文新增「源码地图」章节，更新项目结构树
- 第三方免责声明（README 中英文）

## [1.0.0] - 2026-08-03

首个正式发布版。从 v6.19 开发至今的功能汇总：

### 新增（核心功能）
- **双角色 AI 桌宠**：V4 Flash（小蓝）/ V4 Pro（大蓝）娘化立绘，独立对话历史与长期记忆
- **AI 对话**：DeepSeek function calling、Markdown 渲染、流式打字机效果、截图粘贴识别（Ctrl+V）
- **工具调用（15+）**：打开软件（语义搜索）/ 查天气 / 查时间 / 系统状态 / 剪贴板 / 锁屏 / 音量 / 屏幕截图 / 进程管理 / 待办 / 提醒 / 番茄钟
- **长期记忆系统**：AI 自动记住用户偏好，可视化管理窗口（筛选/搜索/编辑/重要度/备份导入）
- **主动关心机制**：空闲问候、久坐提醒、早安日报、链式唤醒与回访
- **提醒与待办**：提醒管理窗口 + 待办管理窗口（表格/添加/勾选/删除/清空）

### 新增（Live2D）
- **Live2D 显示模式**：右键切换静态立绘 / Live2D，透明 GL 内嵌、视线跟随光标、点击触发动作
- **Live2D 模型库**：自动扫描 `assets/live2d/`，菜单选用任意 `.model3.json` 模型（支持自输入人物模型）
- **动画驱动**：眨眼/呼吸/头部摆动 30fps，参数驱动眼球跟随（EyeBall 满幅 + 头部 ±12°）

### 新增（系统与体验）
- **国际化**：中英双语（`config.language` 切换），覆盖菜单/AI 回复/台词/状态提示
- **输入感知系统**：打盹检测、久坐提醒、光标跟随
- **截图 OCR**：Windows 自带 OCR（零依赖）
- **语义搜索**：本地应用索引（开始菜单 + 注册表 331 个应用，缓存 24h）+ 智能匹配（别名/精确/子串/拼音/模糊）
- **性能**：启动提速 5.3 倍（2.9s → 0.55s，立绘懒加载 + 缩放缓存）
- **打包发布**：PyInstaller onedir 发布包，首次运行引导配置

### 文档
- 双语 README（中/英）+ 项目徽章
- Live2D 自生成人物完整流程文档（Seedream 提示词模板 + PS 拆层手册 + Cubism 绑定手册）
- 第三方免责声明（与 DeepSeek 公司无关联）

## [v6.19] - 2026-07-29（开发起点）

- 项目创建：PySide6 + DeepSeek function calling 双角色 AI 桌宠
- 许可证与作者署名（Binyu Li）

<!--
开发期间的每个 v6.x 小版本均为渐进式迭代，主要变更已并入上方 1.0.0 汇总。
完整 commit 历史见 git log。
-->

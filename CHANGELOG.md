# Changelog

本项目所有重要变更记录于此文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.2.0] - 2026-09-12

模型管理界面（Phase 2）：档案可增删改，并能一键拉取官方模型列表、做连通性自检。

### 新增
- **模型管理对话框**（`model_manager_ui.py`）：右键菜单 → ⚙️ 设置 → 🎯 模型管理…
  - 档案列表 + 新增 / 复制 / 删除（至少保留一份）
  - 逐项编辑：显示名、模型 ID、接口地址、副标题、主题色、温度、输出上限、思考开关、价格（输入/缓存/输出）
  - **拉取官方模型列表**：`GET /models`，下拉选择直接填进「模型 ID」，不用手打
  - **连通性自检**：发 `max_tokens=1` 的最小请求，回显「响应里的真实模型 + 耗时」；若官方已把请求的 ID 指向别的模型会明确提示
  - 恢复出厂（当前档案）
- **思考模式 / 采样温度进右键菜单**：原先只从 config.json 读、菜单里没有入口；现写入当前角色的模型档案（热加载生效）
- `deepseek_client.models_url / list_models / probe_model`：模型列表与连通性探测的网络层（地址由档案 endpoint 推导，换中转/代理时跟着走）
- 回归新增 H13（对话框保存/恢复出厂/增删/防删空）与 H14（模型列表地址推导），共 32 项

### 变更
- 「⚙️ 设置 → 🎯 角色模型」子菜单（逐角色改 ID）由统一的「🎯 模型管理…」入口取代

## [2.1.0] - 2026-09-11

模型身份配置化：把写死在代码里的模型 ID / 显示名 / 接口地址 / 参数 / 价格
收敛成一份用户可编辑的模型档案。

### 新增
- **模型档案 `models.json`**（`model_registry.py`）：每个模型一条档案，含显示名、模型 ID、别名、接口地址、温度/输出上限/思考开关、价格、外观与人设。首次运行自动生成，并从旧 `config.json` 的 `model_flash`/`model_pro`/`reasoning`/`temperature`/`max_tokens` 迁移
- **模型身份单一来源**：`CHARACTERS` 改为运行时从档案构建（结构与旧字典完全兼容，下游 15 处用法无需改动）；右键菜单/托盘的“角色”与“模型”列表也由档案生成——**加一条档案就多一个角色，不用改代码**
- **接口地址统一**：原先 `deepseek_client.py` / `care_engine.py` / `desktop_pet.py` 三处各写一份 URL，现均从档案的 `endpoint` 取（全项目只剩一个默认值定义）
- **价格未知显式标记**：查不到价格的模型在统计条目里带 `price_unknown`，不再静默套用兜底价
- `tests/test_model_registry.py`：档案注册表 18 项单测（首次生成 / 迁移 / 改名升级 / 损坏回退 / 价格 / 兼容层 / 增删 / 落盘往返）
- `models.json.example`：档案模板（供开源使用者参考）

### 修复
- **滚动摘要从来不执行**（`_summarize_old`）：函数内引用了另一个作用域才有的 `cur_model`，抛出的 `NameError` 被 `except Exception: pass` 吞掉——导致对话摘要从不生成、最旧 10 条从不裁剪、上下文无限增长。改为复用 `_extract_chat`，顺带少一份裸写 URL
- **Pro 用量按 Flash 价计费**：流式路径记录用量时未传模型名（`model=''`），一律落到兜底价；两者单价差 3 倍。补上 `fallback_model`
- **价格表因官方重命名失效**：官方将 `deepseek-v4-flash` 重命名为 `deepseek-flash` 后，响应的 `model` 字段与请求写的 ID 不一致，而价格表以请求 ID 为键 → 查不到、静默落兜底价。现按模型档案的规范 ID + 别名归一化后查询
- **输出上限三处互相矛盾**：读取时夾 64000、菜单写 128000、AI 工具写 128000，导致“选 128000 → 存进去 → 重载被压回 64000”，菜单勾选态也对不上。统一为 `model_registry.MAX_OUTPUT_TOKENS`（384000，对齐官方输出上限）

### 变更
- 模型设置弹窗改为写模型档案（不再往 `config.json` 写重复字段），菜单名改按档案显示名展示
- `reasoning`（思考开关）/ `temperature`（采样温度）改由档案提供（原先只从 config.json 读、右键菜单无入口）
- `.gitignore` 新增 `models.json`（本机配置，含个人模型/接口设置）

## [2.0.0] - 2026-08-29

重大版本：好感度与成长系统 + 15 款小游戏大厅 + 动作素材扩充。

### 新增（好感度与成长系统）
- **好感度引擎**（`affection_engine.py`）：双轴养成——好感度 0~200（情感亲密度）+ 等级/XP（陪伴资历）；事件驱动纯积累制（对话/任务/喂食/小游戏/早晚安/陪伴时长），零衰减零惩罚，三重防刷（冷却/单日上限/封顶）；6 称号 + XP 曲线；JSON 原子持久化
- **五阶段人设进化**：初见→熟悉→亲密→依赖→灵魂伴侣，system prompt 随好感度动态注入（称呼/语气/关心强度逐级进化，双角色独立）
- **三层记忆**：新增事件记忆层（`memory_events.py` 回忆日志，200 条滚动，prompt 注入最近 3 条）
- **关系面板**（`affection_ui.py`）：好感度进度条 + 阶段徽章 + 等级/XP + 称号 + 统计
- **回忆相册**：按时间线浏览共同经历（里程碑/事件/标记，含当时好感度）
- **情感选项（galgame 选择支）**：AI 用 `offer_choices` 工具在抉择时刻给出 2~3 个可点击选项，支持 {text, affect} 情感标签（❤️+n），点击即发送并加好感；选择后按钮禁用防重复
- **余额动画气泡**：API 费用角色头顶上浮渐隐（>¥0.1 红色）
- **饱食度系统**：时间戳衰减，喂食恢复 + 好感（冷却 30 分钟），低饱食只提示不惩罚
- **高分里程碑**：每款游戏独立最高分，破纪录 → 桌宠庆祝 + 回忆日志 + 额外好感

### 新增（小游戏大厅，7 → 15 款）
- `pet_minigames.py` 注册式框架：BaseGame 统一难度选择 + 结果带分回调 + 桌宠表情区 + 规则说明按钮
- **新增 8 款**：井字棋（三档 AI）/ Farkle 骰子（天国拯救同款，目标分 500~10000 自选）/ 打地鼠 / 21 点 / 数独 / 俄罗斯方块 / 华容道 / 西蒙记忆
- **已有 7 款加难度/玩法**：五子棋三档 AI、2048 多档（4×4/5×5/4096）、扫雷经典三档、贪吃蛇速度三档+黄金食物、记忆翻牌 6/8/12 对、猜数字范围/限次三档、石头剪刀布三局两胜+桌宠记仇
- **Farkle 计分对齐 KCD 官方**：顺子 123456=1500 / 12345=500 / 23456=750 / 四五六同 ×2/×4/×8 / 单1=100 单5=50（取消三对子/双三同变种奖励）
- 全部游戏带 📖 规则说明 + 难度可选 + 桌宠表情实时反应

### 新增（动作素材）
- 12 个新动作状态（饿/胜利/亲亲/沮丧/跳舞/唱歌/大哭/惊讶/得意/求摸头/撒娇抱抱/送花），双角色齐全（每角色 37 状态），asset() 缺失自动兜底
- 触发联动：饱食度低→饥饿图；小游戏胜/负→欢呼/沮丧图；喂食→亲亲图
- 互动菜单精简：常用 5 动作直显，其余收「🎬 更多动作」子菜单

### 修复
- 贴边状态疯狂左右抽动（animate 贴边定位与摆头 move 冲突）
- 扫雷棋盘空白（QRect 未导入致绘制崩溃）、2048 移动方向反转
- 情感选项可重复点击发送（点击后整组禁用）
- Farkle 无点选交互重做（点击骰子选中/保留选中/全保留重掷/锁定）+ 计分重复 bug
- 21 点自动发牌 + 牌面 str 转换崩溃
- 游戏窗口关闭后定时器泄漏（后台继续跑/突弹结算窗）+ singleShot 回调防 GC
- 键盘游戏键位冲突（难度下拉抢焦点，↑↓切难度/空格弹下拉 → showEvent 统一抢回焦点）

### 工程
- 好感度/回忆/API 统计等运行数据加入 .gitignore；好感度与成长系统/小游戏扩展设计方案文档入库

## [1.1.1] - 2026-08-06

数据安全 + AI 请求健壮性小版本（基于全栈开发知识调研落地）。

### 修复（数据安全）
- **JSON 原子写保护**：所有持久化文件（聊天记忆/事实记忆/待办/提醒/config/应用索引）改为「写 .tmp 临时文件 → os.replace 原子替换」，程序崩溃/断电不再损坏原文件（原直接 open('w') 会截断内容，损坏后静默丢失数据）
- 新增 _atomic_write_json 统一入口，8 个保存点全部切换

### 改进（AI 请求）
- **指数退避重试**：网络/服务繁忙重试等待从固定 5 秒改为 5 秒→10 秒（第 1 次 5 秒、第 2 次 10 秒），状态栏提示具体等待秒数与第几次重试

### 说明
- 数据量评估后暂不迁移 SQLite（聊天记忆 50+300 条上限、最大文件约 127KB，JSON 量级可控；迁移动面大收益低），等聊天记录全量持久化需求出现再评估

## [1.1.0] - 2026-08-04

重大更新：AI 自我管理 + 联网 + 完整附件系统。

### 新增（AI 能力）
- **联网搜索**：web_search 工具（Tavily API，免费 1000 次/月），AI 知识截止 2024.8 后自动联网查最新信息；设置菜单可配 key
- **AI 自我管理三阶段**：read_file（读自己代码自查）/ write_config（白名单改配置热加载）/ edit_own_code（git 保护改自己代码，语法验证+失败不落盘）
- **write_file**：AI 主动生成文件（统一写入 输出/ 目录，防路径穿越）
- **Token 上限扩展**：档位至 128000（对齐 DeepSeek 输出上限 384K）
- **API 自动重试**：503/429/5xx 等 5 秒重试 2 次，状态栏提示

### 新增（会话与附件）
- **会话管理重做**：显示历史与 LLM 上下文分离持久化，重启回显 30 条 + "显示更多"分批加载
- **提醒持久化**：关机不丢，开机补发错过提醒/回访到聊天面板
- **导出聊天**：可视化勾选要导出的消息 + 可选保存位置 + 默认 聊天记录/ 文件夹
- **统一附件系统**：📎 按钮/拖放/Ctrl+V 粘贴全部先暂存为附件卡片栏（图标+文件名+大小+✕），Enter 一起发送
- **常用文件识别**：txt/md/xml/svg/代码类直接读；docx(标准库 zip+XML)/pdf(pypdf)/xlsx/pptx 提取文本；图片 OCR
- **OCR 截图快捷键**：Ctrl+Alt+D 全屏截图→识别→AI 分析；截图/文件卡片化显示（不刷长串文字）

### 修复
- 聊天面板超屏（输入框被吞）；气泡被人物遮挡；随机动作重复；睡觉状态机完整修复（情绪/贴边/切角色）
- 导出黑箱（改为显示历史导出）；回显 bug（历史从未真正回显）；AI 回复不进显示历史
- 闭眼贴边图全套（Seedream 生成 + 连通域保护抠图）
- 自启动改快捷方式形态；启动实测 2.1s

---

## [1.0.1] - 2026-08-03

工程化与文档完善（无功能变更）：

### 新增
- `requirements.txt` / `pyproject.toml`：依赖声明与项目元数据
- `tests/smoke_test.py`：冒烟测试（导入/构造/核心函数，6 项通过）
- `CONTRIBUTING.md`：贡献指南（含源码阅读地图）
- `SECURITY.md` / `CODE_OF_CONDUCT.md` / `.editorconfig`
- `.github/`：Issue 模板（Bug/Feature）+ PR 模板
- README 中英文新增「源码地图」章节，更新项目结构树
- 第三方免责声明（README 中英文）

## [1.0.0] - 2026-08-03

首个正式发布版。从 v6.19 开发至今的功能汇总：

### 新增（核心功能）
- **双角色 AI 桌宠**：V4 Flash（小蓝）/ V4 Pro（大蓝）娘化立绘，独立对话历史与长期记忆
- **AI 对话**：DeepSeek function calling、Markdown 渲染、流式打字机效果、截图粘贴识别（Ctrl+V）
- **工具调用（15+）**：打开软件（语义搜索）/ 查天气 / 查时间 / 系统状态 / 剪贴板 / 锁屏 / 音量 / 屏幕截图 / 进程管理 / 待办 / 提醒 / 番茄钟
- **长期记忆系统**：AI 自动记住用户偏好，可视化管理窗口（筛选/搜索/编辑/重要度/备份导入）
- **主动关心机制**：空闲问候、久坐提醒、早安日报、链式唤醒与回访
- **提醒与待办**：提醒管理窗口 + 待办管理窗口（表格/添加/勾选/删除/清空）

### 新增（Live2D）
- **Live2D 显示模式**：右键切换静态立绘 / Live2D，透明 GL 内嵌、视线跟随光标、点击触发动作
- **Live2D 模型库**：自动扫描 `assets/live2d/`，菜单选用任意 `.model3.json` 模型（支持自输入人物模型）
- **动画驱动**：眨眼/呼吸/头部摆动 30fps，参数驱动眼球跟随（EyeBall 满幅 + 头部 ±12°）

### 新增（系统与体验）
- **国际化**：中英双语（`config.language` 切换），覆盖菜单/AI 回复/台词/状态提示
- **输入感知系统**：打盹检测、久坐提醒、光标跟随
- **截图 OCR**：Windows 自带 OCR（零依赖）
- **语义搜索**：本地应用索引（开始菜单 + 注册表 331 个应用，缓存 24h）+ 智能匹配（别名/精确/子串/拼音/模糊）
- **性能**：启动提速 5.3 倍（2.9s → 0.55s，立绘懒加载 + 缩放缓存）
- **打包发布**：PyInstaller onedir 发布包，首次运行引导配置

### 文档
- 双语 README（中/英）+ 项目徽章
- Live2D 自生成人物完整流程文档（Seedream 提示词模板 + PS 拆层手册 + Cubism 绑定手册）
- 第三方免责声明（与 DeepSeek 公司无关联）

## [v6.19] - 2026-07-29（开发起点）

- 项目创建：PySide6 + DeepSeek function calling 双角色 AI 桌宠
- 许可证与作者署名（Binyu Li）

<!--
开发期间的每个 v6.x 小版本均为渐进式迭代，主要变更已并入上方 1.0.0 汇总。
完整 commit 历史见 git log。
-->

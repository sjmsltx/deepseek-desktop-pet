# 安全策略（Security Policy）

## 支持范围

| 版本 | 支持状态 |
|------|---------|
| **2.7.x（最新发布）** | ✅ 提供安全修复 |
| 2.6.x 及更早 | ⚠️ 不再单独修复，建议升级到最新发布版 |
| 源码 `master` | ✅ 随开发更新 |

> 发布版本号见 [CHANGELOG.md](CHANGELOG.md)；源码注释里的 `v6.xx` 是**内部开发迭代号**，与发布号不是同一串。

## 报告漏洞

**请勿公开提交 Issue。** 推荐路径：

- **GitHub Security Advisory**（推荐）：仓库 → **Security** → *Report a vulnerability* —— 可私密讨论并协同修复
- 邮件：仓库 About 页维护者联系方式

## 报告内容

1. 漏洞类型与影响范围（本地 / 远程，是否需要用户交互）
2. 复现步骤（尽量精简、可稳定复现）
3. 受影响版本与运行环境
4. 可能的修复建议（可选）
5. 是否愿意公开致谢（可选）

我们尽力在 **7 天内**初次响应。

## 设计上的安全模型（本地优先）

本项目是**本地运行**的桌面工具，安全模型围绕这一点设计：

- **API Key 只存在于你本机**：`config.json` / `models.json`（不写死在代码里、不随插件分发）
- **不要提交或分享配置文件**：`config.json`、`models.json`、`memory.json`、`todos.json`、`logs/`、`games_save/` 已在 `.gitignore` 中忽略
- **插件加载有三道闸**：JSON 校验 + Python 语法校验 + 危险操作关键词扫描（`os.system`/`subprocess`/`rmtree` 等会被拒绝加载）
- **自改代码（AI 修改自身源码）**：改动前**先写备份**，改动后走**语法校验**；失败会明确报错，不会静默"假装成功"
- **危险动作需确认**：删除、关机、格式化等由用户确认后才执行
- **语言模型输出不可信**：不要直接运行 AI 生成的、你自己没看懂的系统命令
- **日志**：默认不写入 API Key；但**日志中的路径可能包含用户名**，对外粘贴前请自行脱敏

## 相关

- 贡献流程见 [CONTRIBUTING.md](CONTRIBUTING.md)
- 社区行为准则见 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)
